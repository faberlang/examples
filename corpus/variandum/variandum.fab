+++
term = "variandum"
kind = "keyword"
category = "async"
canonical = true
summary = "Awaits a promissum and binds a mutable name."
syntax = "variandum T name ← future"
aliases = ["await_var"]
related = ["figendum", "tacebit", "fiet", "promissum"]
+++

# =============================================================================
# variandum — Await-bind mutable
# =============================================================================
#
# What this teaches:
#   • Await a `promissum<T>` and bind the success value mutably.
#
# See also: figendum, tacebit, fiet, promissum
# =============================================================================

```fab
functio responde() fiet → numerus {
    redde 3
}

incipiet {
    variandum numerus responsum ← responde()
    responsum ← responsum + 1
    nota responsum
}
```
