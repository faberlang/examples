+++
locale = "la"
+++

# =============================================================================
# regex ↦ modulus — conversio coverage matrix measurement fixture
# =============================================================================
#
# Purpose: measure the real regex ↦ modulus verdict (frontend acceptance,
# MIR lowerability per target, HIR emit arm) for the family pair cell.
#
# GENERATED by scripta/generate-conversio-fixtures.py — do not hand-edit
# =============================================================================
incipit {
    fixum regex src ← "hi" ↦ regex
    fixum _ r ← src ↦ modulus<u32>
}
