[package]
name = "faber-sqlite-binding"
version = "0.1.0"
edition = "2021"

[lib]
path = "shim.rs"

[dependencies]
faber = { package = "faber-runtime", path = "../../../faber-runtime" }
rusqlite = { version = "0.32", features = ["bundled"] }
sha2 = "0.10"

[workspace]
