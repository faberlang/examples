// @ts-nocheck
// Generated by radix - do not edit

import { triga } from "./triga-triga.js";

class SceneHandle {
    index!: number;
    generation!: number;
}

class ResourceHandle {
    index!: number;
    generation!: number;
}

class ResourceTransition {
    previous!: ResourceHandle;
    current!: ResourceHandle;
    changed!: boolean;
}

class ResourceLifecycleTransition {
    logical_index!: number;
    previous!: ResourceHandle;
    current!: ResourceHandle;
    changed!: boolean;
    removed!: boolean;
}

class ResourceLifecycleBatchFacts {
    transition_count!: number;
    changed_count!: number;
    unchanged_count!: number;
    created_count!: number;
    removed_count!: number;
    live_count!: number;
}

type SceneNodeKind = { tag: "SceneGroup" } | { tag: "SceneMesh", geometry: ResourceHandle, material: ResourceHandle } | { tag: "SceneCamera" } | { tag: "SceneLight" };
const SceneGroup: SceneNodeKind = { tag: "SceneGroup" };
function SceneMesh(geometry: ResourceHandle, material: ResourceHandle): SceneNodeKind { return { tag: "SceneMesh", geometry, material }; }
const SceneCamera: SceneNodeKind = { tag: "SceneCamera" };
const SceneLight: SceneNodeKind = { tag: "SceneLight" };

class SceneNode {
    name!: string;
    kind!: SceneNodeKind;
    parent!: SceneHandle;
    children!: Array<SceneHandle>;
    visible!: boolean;
    local_matrix!: any;
    world_matrix!: any;
    world_dirty!: boolean;
}

class SceneSlot {
    generation!: number;
    alive!: boolean;
    node!: SceneNode;
}

class SceneStore {
    slots!: Array<SceneSlot>;
}

class SceneInsert {
    store!: SceneStore;
    handle!: SceneHandle;
}

class SceneTraversal {
    handles!: Array<SceneHandle>;
    remaining!: number;
}

class SceneCount {
    count!: number;
    remaining!: number;
}

class SceneMeshResources {
    node!: SceneHandle;
    geometry!: ResourceHandle;
    material!: ResourceHandle;
}

class SceneMeshTransformPayload {
    node!: SceneHandle;
    payload!: any;
}

class SceneMeshDrawPacket {
    node!: SceneHandle;
    geometry!: ResourceHandle;
    material!: ResourceHandle;
    transform!: any;
}

class SceneVisibleMeshDrawBatchFacts {
    packet_count!: number;
    transform_payload_byte_count!: number;
}

class SceneVisibleMeshResourceFacts {
    mesh_count!: number;
    resource_pair_count!: number;
    geometry_handle_count!: number;
    material_handle_count!: number;
}

class SceneMeshResourceTraversal {
    resources!: Array<SceneMeshResources>;
    remaining!: number;
}

class ResourceHandleTraversal {
    handles!: Array<ResourceHandle>;
    remaining!: number;
}

function scene_handle_equals(a: SceneHandle, b: SceneHandle): boolean {
    return (((a.index as number) === (b.index as number)) && ((a.generation as number) === (b.generation as number)));
}
function resource_handle_equals(a: ResourceHandle, b: ResourceHandle): boolean {
    return (((a.index as number) === (b.index as number)) && ((a.generation as number) === (b.generation as number)));
}
function resource_handle_next(handle: ResourceHandle): ResourceHandle {
    return Object.assign(new ResourceHandle(), { index: handle.index, generation: (handle.generation + 1) });
}
function resource_transition_unchanged(handle: ResourceHandle): ResourceTransition {
    const stable: ResourceHandle = Object.assign(new ResourceHandle(), { index: handle.index, generation: handle.generation });
    return Object.assign(new ResourceTransition(), { previous: stable, current: stable, changed: false });
}
function resource_transition_replaced(handle: ResourceHandle): ResourceTransition {
    const previous: ResourceHandle = Object.assign(new ResourceHandle(), { index: handle.index, generation: handle.generation });
    return Object.assign(new ResourceTransition(), { previous: previous, current: resource_handle_next(previous), changed: true });
}
function resource_transition_valid(transition: ResourceTransition): boolean {
    if (((transition.previous.index as number) !== (transition.current.index as number))) {
        return false;
    }
    if (transition.changed) {
        return ((transition.current.generation as number) === ((transition.previous.generation + 1) as number));
    }
    return ((transition.current.generation as number) === (transition.previous.generation as number));
}
function _resource_transition_index_seen(transitions: Array<ResourceTransition>, limit: number, resource_index: number): boolean {
    let i: number = 0;
    while ((i < limit)) {
        const transition: ResourceTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (((transition.current.index as number) === (resource_index as number))) {
            return true;
        }
        i = (i + 1);
    }
    return false;
}
function resource_transitions_valid(transitions: Array<ResourceTransition>): boolean {
    let i: number = 0;
    while ((i < transitions.length)) {
        const transition: ResourceTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (((resource_transition_valid(transition) as boolean) === (false as boolean))) {
            return false;
        }
        if (_resource_transition_index_seen(transitions, i, transition.current.index)) {
            return false;
        }
        i = (i + 1);
    }
    return true;
}
function resource_transition_changed_count(transitions: Array<ResourceTransition>): number | null {
    if (((resource_transitions_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let count: number = 0;
    let i: number = 0;
    while ((i < transitions.length)) {
        const transition: ResourceTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (transition.changed) {
            count = (count + 1);
        }
        i = (i + 1);
    }
    return count;
}
function resource_transition_unchanged_count(transitions: Array<ResourceTransition>): number | null {
    if (((resource_transitions_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let count: number = 0;
    let i: number = 0;
    while ((i < transitions.length)) {
        const transition: ResourceTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (((transition.changed as boolean) === (false as boolean))) {
            count = (count + 1);
        }
        i = (i + 1);
    }
    return count;
}
function resource_transition_changed_indices(transitions: Array<ResourceTransition>): Array<number> | null {
    if (((resource_transitions_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let indices: Array<number> = [];
    let i: number = 0;
    while ((i < transitions.length)) {
        const transition: ResourceTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (transition.changed) {
            indices.push(transition.current.index);
        }
        i = (i + 1);
    }
    return indices;
}
function resource_transition_changed_handles(transitions: Array<ResourceTransition>): Array<ResourceHandle> | null {
    if (((resource_transitions_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let handles: Array<ResourceHandle> = [];
    let i: number = 0;
    while ((i < transitions.length)) {
        const transition: ResourceTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (transition.changed) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current.index, generation: transition.current.generation }));
        }
        i = (i + 1);
    }
    return handles;
}
function resource_transition_current_handles(transitions: Array<ResourceTransition>): Array<ResourceHandle> | null {
    if (((resource_transitions_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let handles: Array<ResourceHandle> = [];
    let i: number = 0;
    while ((i < transitions.length)) {
        const transition: ResourceTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        handles.push(Object.assign(new ResourceHandle(), { index: transition.current.index, generation: transition.current.generation }));
        i = (i + 1);
    }
    return handles;
}
function resource_transition_unchanged_indices(transitions: Array<ResourceTransition>): Array<number> | null {
    if (((resource_transitions_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let indices: Array<number> = [];
    let i: number = 0;
    while ((i < transitions.length)) {
        const transition: ResourceTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (((transition.changed as boolean) === (false as boolean))) {
            indices.push(transition.current.index);
        }
        i = (i + 1);
    }
    return indices;
}
function resource_transition_unchanged_handles(transitions: Array<ResourceTransition>): Array<ResourceHandle> | null {
    if (((resource_transitions_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let handles: Array<ResourceHandle> = [];
    let i: number = 0;
    while ((i < transitions.length)) {
        const transition: ResourceTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (((transition.changed as boolean) === (false as boolean))) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current.index, generation: transition.current.generation }));
        }
        i = (i + 1);
    }
    return handles;
}
function resource_lifecycle_unchanged(handle: ResourceHandle): ResourceLifecycleTransition {
    return Object.assign(new ResourceLifecycleTransition(), { logical_index: handle.index, previous: handle, current: handle, changed: false, removed: false });
}
function resource_lifecycle_replaced(handle: ResourceHandle): ResourceLifecycleTransition {
    return Object.assign(new ResourceLifecycleTransition(), { logical_index: handle.index, previous: handle, current: resource_handle_next(handle), changed: true, removed: false });
}
function resource_lifecycle_removed(handle: ResourceHandle): ResourceLifecycleTransition {
    return Object.assign(new ResourceLifecycleTransition(), { logical_index: handle.index, previous: handle, current: null, changed: true, removed: true });
}
function resource_lifecycle_created(handle: ResourceHandle): ResourceLifecycleTransition {
    return Object.assign(new ResourceLifecycleTransition(), { logical_index: handle.index, previous: null, current: handle, changed: true, removed: false });
}
function resource_lifecycle_valid(transition: ResourceLifecycleTransition): boolean {
    if (((transition.previous === null) && (transition.current === null))) {
        return false;
    }
    if ((transition.previous !== null)) {
        if (((transition.previous!.index as number) !== (transition.logical_index as number))) {
            return false;
        }
    }
    if ((transition.current !== null)) {
        if (((transition.current!.index as number) !== (transition.logical_index as number))) {
            return false;
        }
    }
    if (transition.removed) {
        return ((transition.changed && (transition.previous !== null)) && (transition.current === null));
    }
    if (((transition.changed as boolean) === (false as boolean))) {
        return (((transition.previous !== null) && (transition.current !== null)) && ((transition.previous!.generation as number) === (transition.current!.generation as number)));
    }
    if ((transition.previous === null)) {
        return (transition.current !== null);
    }
    if ((transition.current === null)) {
        return false;
    }
    return ((transition.current!.generation as number) === ((transition.previous!.generation + 1) as number));
}
function _resource_lifecycle_index_seen(transitions: Array<ResourceLifecycleTransition>, limit: number, logical_index: number): boolean {
    let i: number = 0;
    while ((i < limit)) {
        const transition: ResourceLifecycleTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (((transition.logical_index as number) === (logical_index as number))) {
            return true;
        }
        i = (i + 1);
    }
    return false;
}
function resource_lifecycles_valid(transitions: Array<ResourceLifecycleTransition>): boolean {
    let i: number = 0;
    while ((i < transitions.length)) {
        const transition: ResourceLifecycleTransition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(transitions, i);
        if (((resource_lifecycle_valid(transition) as boolean) === (false as boolean))) {
            return false;
        }
        if (_resource_lifecycle_index_seen(transitions, i, transition.logical_index)) {
            return false;
        }
        i = (i + 1);
    }
    return true;
}
function resource_lifecycle_changed_count(transitions: Array<ResourceLifecycleTransition>): number | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let count: number = 0;
    for (const transition of transitions) {
        if (transition.changed) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_unchanged_count(transitions: Array<ResourceLifecycleTransition>): number | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let count: number = 0;
    for (const transition of transitions) {
        if (((transition.changed as boolean) === (false as boolean))) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_created_count(transitions: Array<ResourceLifecycleTransition>): number | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let count: number = 0;
    for (const transition of transitions) {
        if (((transition.previous === null) && (transition.current !== null))) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_removed_count(transitions: Array<ResourceLifecycleTransition>): number | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let count: number = 0;
    for (const transition of transitions) {
        if (transition.removed) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_live_count(transitions: Array<ResourceLifecycleTransition>): number | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let count: number = 0;
    for (const transition of transitions) {
        if ((transition.current !== null)) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_batch_facts(transitions: Array<ResourceLifecycleTransition>): ResourceLifecycleBatchFacts | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let changed_count: number = 0;
    let unchanged_count: number = 0;
    let created_count: number = 0;
    let removed_count: number = 0;
    let live_count: number = 0;
    for (const transition of transitions) {
        if (transition.changed) {
            changed_count = (changed_count + 1);
        }
        if (((transition.changed as boolean) === (false as boolean))) {
            unchanged_count = (unchanged_count + 1);
        }
        if (((transition.previous === null) && (transition.current !== null))) {
            created_count = (created_count + 1);
        }
        if (transition.removed) {
            removed_count = (removed_count + 1);
        }
        if ((transition.current !== null)) {
            live_count = (live_count + 1);
        }
    }
    return Object.assign(new ResourceLifecycleBatchFacts(), { transition_count: transitions.length, changed_count: changed_count, unchanged_count: unchanged_count, created_count: created_count, removed_count: removed_count, live_count: live_count });
}
function resource_lifecycle_changed_indices(transitions: Array<ResourceLifecycleTransition>): Array<number> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let indices: Array<number> = [];
    for (const transition of transitions) {
        if (transition.changed) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_unchanged_indices(transitions: Array<ResourceLifecycleTransition>): Array<number> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let indices: Array<number> = [];
    for (const transition of transitions) {
        if (((transition.changed as boolean) === (false as boolean))) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_removed_indices(transitions: Array<ResourceLifecycleTransition>): Array<number> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let indices: Array<number> = [];
    for (const transition of transitions) {
        if (transition.removed) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_live_indices(transitions: Array<ResourceLifecycleTransition>): Array<number> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let indices: Array<number> = [];
    for (const transition of transitions) {
        if ((transition.current !== null)) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_retired_indices(transitions: Array<ResourceLifecycleTransition>): Array<number> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let indices: Array<number> = [];
    for (const transition of transitions) {
        if ((transition.changed && (transition.previous !== null))) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_changed_handles(transitions: Array<ResourceLifecycleTransition>): Array<ResourceHandle> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let handles: Array<ResourceHandle> = [];
    for (const transition of transitions) {
        if ((transition.changed && (transition.current !== null))) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current!.index, generation: transition.current!.generation }));
        }
    }
    return handles;
}
function resource_lifecycle_unchanged_handles(transitions: Array<ResourceLifecycleTransition>): Array<ResourceHandle> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let handles: Array<ResourceHandle> = [];
    for (const transition of transitions) {
        if ((((transition.changed as boolean) === (false as boolean)) && (transition.current !== null))) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current!.index, generation: transition.current!.generation }));
        }
    }
    return handles;
}
function resource_lifecycle_removed_handles(transitions: Array<ResourceLifecycleTransition>): Array<ResourceHandle> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let handles: Array<ResourceHandle> = [];
    for (const transition of transitions) {
        if ((transition.removed && (transition.previous !== null))) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.previous!.index, generation: transition.previous!.generation }));
        }
    }
    return handles;
}
function resource_lifecycle_retired_handles(transitions: Array<ResourceLifecycleTransition>): Array<ResourceHandle> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let handles: Array<ResourceHandle> = [];
    for (const transition of transitions) {
        if ((transition.changed && (transition.previous !== null))) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.previous!.index, generation: transition.previous!.generation }));
        }
    }
    return handles;
}
function resource_lifecycle_current_handles(transitions: Array<ResourceLifecycleTransition>): Array<ResourceHandle> | null {
    if (((resource_lifecycles_valid(transitions) as boolean) === (false as boolean))) {
        return null;
    }
    let handles: Array<ResourceHandle> = [];
    for (const transition of transitions) {
        if ((transition.current !== null)) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current!.index, generation: transition.current!.generation }));
        }
    }
    return handles;
}
function scene_store(): SceneStore {
    return Object.assign(new SceneStore(), { slots: [] });
}
function scene_node(name: string, kind: SceneNodeKind): SceneNode {
    return Object.assign(new SceneNode(), { name: name, kind: kind, children: [], visible: true, local_matrix: triga.matrix4_identitas(), world_matrix: triga.matrix4_identitas(), world_dirty: true });
}
function scene_group(name: string): SceneNode {
    return scene_node(name, SceneGroup);
}
function scene_mesh(name: string, geometry: ResourceHandle, material: ResourceHandle): SceneNode {
    return scene_node(name, SceneMesh(Object.assign(new ResourceHandle(), { index: geometry.index, generation: geometry.generation }), Object.assign(new ResourceHandle(), { index: material.index, generation: material.generation })));
}
function scene_camera(name: string): SceneNode {
    return scene_node(name, SceneCamera);
}
function scene_light(name: string): SceneNode {
    return scene_node(name, SceneLight);
}
function scene_mesh_geometry(node: SceneNode): ResourceHandle | null {
    {
        let t1365: SceneNodeKind = ((value: SceneNodeKind): SceneNodeKind => value)(node.kind);
        if (t1365.tag === "SceneMesh") {
            const geometry = t1365.geometry;
            const material = t1365.material;
            return geometry;
        }
        else if (t1365.tag === "SceneGroup") {
            return null;
        }
        else if (t1365.tag === "SceneCamera") {
            return null;
        }
        else if (t1365.tag === "SceneLight") {
            return null;
        }
        else {
            throw new Error("non-exhaustive discerne");
        }}
}
function scene_mesh_material(node: SceneNode): ResourceHandle | null {
    {
        let t1382: SceneNodeKind = ((value: SceneNodeKind): SceneNodeKind => value)(node.kind);
        if (t1382.tag === "SceneMesh") {
            const geometry = t1382.geometry;
            const material = t1382.material;
            return material;
        }
        else if (t1382.tag === "SceneGroup") {
            return null;
        }
        else if (t1382.tag === "SceneCamera") {
            return null;
        }
        else if (t1382.tag === "SceneLight") {
            return null;
        }
        else {
            throw new Error("non-exhaustive discerne");
        }}
}
function scene_mesh_world_matrix(store: SceneStore, handle: SceneHandle): any | null {
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    {
        let t1412: SceneNodeKind = ((value: SceneNodeKind): SceneNodeKind => value)(node_result!.kind);
        if (t1412.tag === "SceneMesh") {
            const geometry = t1412.geometry;
            const material = t1412.material;
            return node_result!.world_matrix;
        }
        else if (t1412.tag === "SceneGroup") {
            return null;
        }
        else if (t1412.tag === "SceneCamera") {
            return null;
        }
        else if (t1412.tag === "SceneLight") {
            return null;
        }
        else {
            throw new Error("non-exhaustive discerne");
        }}
}
function scene_insert(store: SceneStore, node: SceneNode): SceneInsert {
    let slots: Array<SceneSlot> = [];
    let i: number = 0;
    while ((i < store.slots.length)) {
        slots.push(((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(store.slots, i));
        i = (i + 1);
    }
    const index: number = slots.length;
    slots.push(Object.assign(new SceneSlot(), { generation: 0, alive: true, node: node }));
    return Object.assign(new SceneInsert(), { store: Object.assign(new SceneStore(), { slots: slots }), handle: Object.assign(new SceneHandle(), { index: index, generation: 0 }) });
}
function scene_contains(store: SceneStore, handle: SceneHandle): boolean {
    if ((handle.index >= store.slots.length)) {
        return false;
    }
    const slot: SceneSlot = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(store.slots, handle.index);
    return (slot.alive && ((slot.generation as number) === (handle.generation as number)));
}
function scene_get(store: SceneStore, handle: SceneHandle): SceneNode | null {
    if (((scene_contains(store, handle) as boolean) === (false as boolean))) {
        return null;
    }
    return ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(store.slots, handle.index).node;
}
function _scene_traversal_contains(handles: Array<SceneHandle>, handle: SceneHandle): boolean {
    let i: number = 0;
    while ((i < handles.length)) {
        const existing: SceneHandle = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(handles, i);
        if (((scene_handle_equals(existing, handle) as boolean) === (true as boolean))) {
            return true;
        }
        i = (i + 1);
    }
    return false;
}
function _scene_traverse_preorder(store: SceneStore, handle: SceneHandle, handles: Array<SceneHandle>, remaining: number): SceneTraversal | null {
    if (((remaining as number) === (0 as number))) {
        return null;
    }
    if (_scene_traversal_contains(handles, handle)) {
        return null;
    }
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    let updated_handles: Array<SceneHandle> = handles;
    updated_handles.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    let traversal: SceneTraversal = Object.assign(new SceneTraversal(), { handles: updated_handles, remaining: (remaining - 1) });
    let i: number = 0;
    while ((i < node.children.length)) {
        const current: SceneTraversal | null = _scene_traverse_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i), traversal.handles, traversal.remaining);
        if ((current === null)) {
            return null;
        }
        const current_value: SceneTraversal = Object.assign(new SceneTraversal(), { handles: current!.handles, remaining: current!.remaining });
        traversal = Object.assign(new SceneTraversal(), { handles: current_value.handles, remaining: current_value.remaining });
        i = (i + 1);
    }
    return traversal;
}
function scene_traverse(store: SceneStore, root: SceneHandle): Array<SceneHandle> | null {
    const traversal: SceneTraversal | null = _scene_traverse_preorder(store, root, [], store.slots.length);
    if ((traversal === null)) {
        return null;
    }
    return traversal!.handles;
}
function _scene_traverse_visible_preorder(store: SceneStore, handle: SceneHandle, handles: Array<SceneHandle>, remaining: number, parent_visible: boolean): SceneTraversal | null {
    if (((remaining as number) === (0 as number))) {
        return null;
    }
    if (_scene_traversal_contains(handles, handle)) {
        return null;
    }
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    const effective_visible: boolean = (parent_visible && node.visible);
    let updated_handles: Array<SceneHandle> = handles;
    if (effective_visible) {
        updated_handles.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    }
    let traversal: SceneTraversal = Object.assign(new SceneTraversal(), { handles: updated_handles, remaining: (remaining - 1) });
    let i: number = 0;
    while ((i < node.children.length)) {
        const current: SceneTraversal | null = _scene_traverse_visible_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i), traversal.handles, traversal.remaining, effective_visible);
        if ((current === null)) {
            return null;
        }
        const current_value: SceneTraversal = Object.assign(new SceneTraversal(), { handles: current!.handles, remaining: current!.remaining });
        traversal = Object.assign(new SceneTraversal(), { handles: current_value.handles, remaining: current_value.remaining });
        i = (i + 1);
    }
    return traversal;
}
function scene_visible_traverse(store: SceneStore, root: SceneHandle): Array<SceneHandle> | null {
    const traversal: SceneTraversal | null = _scene_traverse_visible_preorder(store, root, [], store.slots.length, true);
    if ((traversal === null)) {
        return null;
    }
    return traversal!.handles;
}
function _scene_traverse_visible_mesh_preorder(store: SceneStore, handle: SceneHandle, handles: Array<SceneHandle>, remaining: number, parent_visible: boolean): SceneTraversal | null {
    if (((remaining as number) === (0 as number))) {
        return null;
    }
    if (_scene_traversal_contains(handles, handle)) {
        return null;
    }
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    const effective_visible: boolean = (parent_visible && node.visible);
    let updated_handles: Array<SceneHandle> = handles;
    if (effective_visible) {
        {
            let t1890: SceneNodeKind = ((value: SceneNodeKind): SceneNodeKind => value)(node.kind);
            if (t1890.tag === "SceneMesh") {
                const geometry = t1890.geometry;
                const material = t1890.material;
                updated_handles.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
            }
            else if (t1890.tag === "SceneGroup") {
            }
            else if (t1890.tag === "SceneCamera") {
            }
            else if (t1890.tag === "SceneLight") {
            }
            else {
                throw new Error("non-exhaustive discerne");
            }}
    }
    let traversal: SceneTraversal = Object.assign(new SceneTraversal(), { handles: updated_handles, remaining: (remaining - 1) });
    let i: number = 0;
    while ((i < node.children.length)) {
        const current: SceneTraversal | null = _scene_traverse_visible_mesh_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i), traversal.handles, traversal.remaining, effective_visible);
        if ((current === null)) {
            return null;
        }
        const current_value: SceneTraversal = Object.assign(new SceneTraversal(), { handles: current!.handles, remaining: current!.remaining });
        traversal = Object.assign(new SceneTraversal(), { handles: current_value.handles, remaining: current_value.remaining });
        i = (i + 1);
    }
    return traversal;
}
function scene_visible_mesh_traverse(store: SceneStore, root: SceneHandle): Array<SceneHandle> | null {
    const traversal: SceneTraversal | null = _scene_traverse_visible_mesh_preorder(store, root, [], store.slots.length, true);
    if ((traversal === null)) {
        return null;
    }
    return traversal!.handles;
}
function _scene_count_visible_mesh_preorder(store: SceneStore, handle: SceneHandle, seen: Array<SceneHandle>, remaining: number, parent_visible: boolean): SceneCount | null {
    if (((remaining as number) === (0 as number))) {
        return null;
    }
    if (_scene_traversal_contains(seen, handle)) {
        return null;
    }
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    const effective_visible: boolean = (parent_visible && node.visible);
    let updated_seen: Array<SceneHandle> = seen;
    updated_seen.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    let count: number = 0;
    if (effective_visible) {
        {
            let t2051: SceneNodeKind = ((value: SceneNodeKind): SceneNodeKind => value)(node.kind);
            if (t2051.tag === "SceneMesh") {
                const geometry = t2051.geometry;
                const material = t2051.material;
                count = 1;
            }
            else if (t2051.tag === "SceneGroup") {
            }
            else if (t2051.tag === "SceneCamera") {
            }
            else if (t2051.tag === "SceneLight") {
            }
            else {
                throw new Error("non-exhaustive discerne");
            }}
    }
    let result: SceneCount = Object.assign(new SceneCount(), { count: count, remaining: (remaining - 1) });
    let i: number = 0;
    while ((i < node.children.length)) {
        const child_count: SceneCount | null = _scene_count_visible_mesh_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i), updated_seen, result.remaining, effective_visible);
        if ((child_count === null)) {
            return null;
        }
        const child_value: SceneCount = Object.assign(new SceneCount(), { count: child_count!.count, remaining: child_count!.remaining });
        result = Object.assign(new SceneCount(), { count: (result.count + child_value.count), remaining: child_value.remaining });
        updated_seen.push(Object.assign(new SceneHandle(), { index: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i).index, generation: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i).generation }));
        i = (i + 1);
    }
    return result;
}
function scene_visible_mesh_count(store: SceneStore, root: SceneHandle): number | null {
    const count_result: SceneCount | null = _scene_count_visible_mesh_preorder(store, root, [], store.slots.length, true);
    if ((count_result === null)) {
        return null;
    }
    return count_result!.count;
}
function _scene_traverse_visible_mesh_resources_preorder(store: SceneStore, handle: SceneHandle, resources: Array<SceneMeshResources>, seen: Array<SceneHandle>, remaining: number, parent_visible: boolean): SceneMeshResourceTraversal | null {
    if (((remaining as number) === (0 as number))) {
        return null;
    }
    if (_scene_traversal_contains(seen, handle)) {
        return null;
    }
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    const effective_visible: boolean = (parent_visible && node.visible);
    let updated_seen: Array<SceneHandle> = seen;
    updated_seen.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    let updated_resources: Array<SceneMeshResources> = resources;
    if (effective_visible) {
        {
            let t2248: SceneNodeKind = ((value: SceneNodeKind): SceneNodeKind => value)(node.kind);
            if (t2248.tag === "SceneMesh") {
                const geometry = t2248.geometry;
                const material = t2248.material;
                updated_resources.push(Object.assign(new SceneMeshResources(), { node: Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }), geometry: Object.assign(new ResourceHandle(), { index: geometry.index, generation: geometry.generation }), material: Object.assign(new ResourceHandle(), { index: material.index, generation: material.generation }) }));
            }
            else if (t2248.tag === "SceneGroup") {
            }
            else if (t2248.tag === "SceneCamera") {
            }
            else if (t2248.tag === "SceneLight") {
            }
            else {
                throw new Error("non-exhaustive discerne");
            }}
    }
    let traversal: SceneMeshResourceTraversal = Object.assign(new SceneMeshResourceTraversal(), { resources: updated_resources, remaining: (remaining - 1) });
    let i: number = 0;
    while ((i < node.children.length)) {
        const current: SceneMeshResourceTraversal | null = _scene_traverse_visible_mesh_resources_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i), traversal.resources, updated_seen, traversal.remaining, effective_visible);
        if ((current === null)) {
            return null;
        }
        const current_value: SceneMeshResourceTraversal = Object.assign(new SceneMeshResourceTraversal(), { resources: current!.resources, remaining: current!.remaining });
        traversal = Object.assign(new SceneMeshResourceTraversal(), { resources: current_value.resources, remaining: current_value.remaining });
        updated_seen.push(Object.assign(new SceneHandle(), { index: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i).index, generation: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i).generation }));
        i = (i + 1);
    }
    return traversal;
}
function scene_visible_mesh_resources(store: SceneStore, root: SceneHandle): Array<SceneMeshResources> | null {
    const traversal: SceneMeshResourceTraversal | null = _scene_traverse_visible_mesh_resources_preorder(store, root, [], [], store.slots.length, true);
    if ((traversal === null)) {
        return null;
    }
    return traversal!.resources;
}
function scene_visible_mesh_resource_pair_count(store: SceneStore, root: SceneHandle): number | null {
    return scene_visible_mesh_count(store, root);
}
function _scene_visible_mesh_resource_handles_preorder(store: SceneStore, handle: SceneHandle, handles: Array<ResourceHandle>, seen: Array<SceneHandle>, remaining: number, parent_visible: boolean, geometry_handles: boolean): ResourceHandleTraversal | null {
    if (((remaining as number) === (0 as number))) {
        return null;
    }
    if (_scene_traversal_contains(seen, handle)) {
        return null;
    }
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    const effective_visible: boolean = (parent_visible && node.visible);
    let updated_seen: Array<SceneHandle> = seen;
    updated_seen.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    let updated_handles: Array<ResourceHandle> = handles;
    if (effective_visible) {
        {
            let t2449: SceneNodeKind = ((value: SceneNodeKind): SceneNodeKind => value)(node.kind);
            if (t2449.tag === "SceneMesh") {
                const geometry = t2449.geometry;
                const material = t2449.material;
                if (geometry_handles) {
                    updated_handles.push(Object.assign(new ResourceHandle(), { index: geometry.index, generation: geometry.generation }));
                } else {
                    updated_handles.push(Object.assign(new ResourceHandle(), { index: material.index, generation: material.generation }));
                }
            }
            else if (t2449.tag === "SceneGroup") {
            }
            else if (t2449.tag === "SceneCamera") {
            }
            else if (t2449.tag === "SceneLight") {
            }
            else {
                throw new Error("non-exhaustive discerne");
            }}
    }
    let traversal: ResourceHandleTraversal = Object.assign(new ResourceHandleTraversal(), { handles: updated_handles, remaining: (remaining - 1) });
    let i: number = 0;
    while ((i < node.children.length)) {
        const current: ResourceHandleTraversal | null = _scene_visible_mesh_resource_handles_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i), traversal.handles, updated_seen, traversal.remaining, effective_visible, geometry_handles);
        if ((current === null)) {
            return null;
        }
        const current_value: ResourceHandleTraversal = Object.assign(new ResourceHandleTraversal(), { handles: current!.handles, remaining: current!.remaining });
        traversal = Object.assign(new ResourceHandleTraversal(), { handles: current_value.handles, remaining: current_value.remaining });
        updated_seen.push(Object.assign(new SceneHandle(), { index: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i).index, generation: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i).generation }));
        i = (i + 1);
    }
    return traversal;
}
function scene_visible_mesh_geometry_handles(store: SceneStore, root: SceneHandle): Array<ResourceHandle> | null {
    const traversal: ResourceHandleTraversal | null = _scene_visible_mesh_resource_handles_preorder(store, root, [], [], store.slots.length, true, true);
    if ((traversal === null)) {
        return null;
    }
    return traversal!.handles;
}
function scene_visible_mesh_material_handles(store: SceneStore, root: SceneHandle): Array<ResourceHandle> | null {
    const traversal: ResourceHandleTraversal | null = _scene_visible_mesh_resource_handles_preorder(store, root, [], [], store.slots.length, true, false);
    if ((traversal === null)) {
        return null;
    }
    return traversal!.handles;
}
function scene_visible_mesh_resource_facts(store: SceneStore, root: SceneHandle): SceneVisibleMeshResourceFacts | null {
    const mesh_count_result: number | null = scene_visible_mesh_count(store, root);
    if ((mesh_count_result === null)) {
        return null;
    }
    const pair_count_result: number | null = scene_visible_mesh_resource_pair_count(store, root);
    if ((pair_count_result === null)) {
        return null;
    }
    const geometry_handles_result: Array<ResourceHandle> | null = scene_visible_mesh_geometry_handles(store, root);
    if ((geometry_handles_result === null)) {
        return null;
    }
    const material_handles_result: Array<ResourceHandle> | null = scene_visible_mesh_material_handles(store, root);
    if ((material_handles_result === null)) {
        return null;
    }
    const empty_geometry_handles: Array<ResourceHandle> = [];
    const empty_material_handles: Array<ResourceHandle> = [];
    const geometry_handles: Array<ResourceHandle> = (geometry_handles_result ?? empty_geometry_handles);
    const material_handles: Array<ResourceHandle> = (material_handles_result ?? empty_material_handles);
    const mesh_count: number = (mesh_count_result ?? 0);
    const pair_count: number = (pair_count_result ?? 0);
    if (((mesh_count as number) !== (pair_count as number))) {
        return null;
    }
    if (((geometry_handles.length as number) !== (mesh_count as number))) {
        return null;
    }
    if (((material_handles.length as number) !== (mesh_count as number))) {
        return null;
    }
    return Object.assign(new SceneVisibleMeshResourceFacts(), { mesh_count: mesh_count, resource_pair_count: pair_count, geometry_handle_count: geometry_handles.length, material_handle_count: material_handles.length });
}
function scene_visible_mesh_transform_payloads(store: SceneStore, root: SceneHandle, view_projection: any): Array<SceneMeshTransformPayload> | null {
    if (((triga.matrix4_valid(view_projection) as boolean) === (false as boolean))) {
        return null;
    }
    const handles: Array<SceneHandle> | null = scene_visible_mesh_traverse(store, root);
    if ((handles === null)) {
        return null;
    }
    const empty_handles: Array<SceneHandle> = [];
    const visible_handles: Array<SceneHandle> = (handles ?? empty_handles);
    let payloads: Array<SceneMeshTransformPayload> = [];
    for (const handle of visible_handles) {
        const world: any | null = scene_mesh_world_matrix(store, handle);
        if ((world === null)) {
            return null;
        }
        const world_matrix: any = (world ?? triga.matrix4_identitas());
        const payload: any | null = triga.transform_payload(world_matrix, view_projection);
        if ((payload === null)) {
            return null;
        }
        const transform_payload: any = (payload ?? Object.assign(new any(), { values: [] }));
        payloads.push(Object.assign(new SceneMeshTransformPayload(), { node: Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }), payload: transform_payload }));
    }
    return payloads;
}
function scene_visible_mesh_transform_payload_count(store: SceneStore, root: SceneHandle, view_projection: any): number | null {
    const payloads: Array<SceneMeshTransformPayload> | null = scene_visible_mesh_transform_payloads(store, root, view_projection);
    if ((payloads === null)) {
        return null;
    }
    const empty_payloads: Array<SceneMeshTransformPayload> = [];
    const visible_payloads: Array<SceneMeshTransformPayload> = (payloads ?? empty_payloads);
    let count: number = 0;
    for (const payload of visible_payloads) {
        count = (count + 1);
    }
    return count;
}
function scene_visible_mesh_transform_payload_byte_count(store: SceneStore, root: SceneHandle, view_projection: any): number | null {
    const payloads: Array<SceneMeshTransformPayload> | null = scene_visible_mesh_transform_payloads(store, root, view_projection);
    if ((payloads === null)) {
        return null;
    }
    const empty_payloads: Array<SceneMeshTransformPayload> = [];
    const visible_payloads: Array<SceneMeshTransformPayload> = (payloads ?? empty_payloads);
    let byte_count: number = 0;
    for (const payload of visible_payloads) {
        const payload_bytes: number | null = triga.transform_payload_byte_count(payload.payload);
        if ((payload_bytes === null)) {
            return null;
        }
        const bytes: number = (payload_bytes ?? 0);
        byte_count = (byte_count + bytes);
    }
    return byte_count;
}
function scene_visible_mesh_draw_packets(store: SceneStore, root: SceneHandle, view_projection: any): Array<SceneMeshDrawPacket> | null {
    const resources_result: Array<SceneMeshResources> | null = scene_visible_mesh_resources(store, root);
    if ((resources_result === null)) {
        return null;
    }
    const payloads_result: Array<SceneMeshTransformPayload> | null = scene_visible_mesh_transform_payloads(store, root, view_projection);
    if ((payloads_result === null)) {
        return null;
    }
    const empty_resources: Array<SceneMeshResources> = [];
    const resources: Array<SceneMeshResources> = (resources_result ?? empty_resources);
    const empty_payloads: Array<SceneMeshTransformPayload> = [];
    const payloads: Array<SceneMeshTransformPayload> = (payloads_result ?? empty_payloads);
    if (((resources.length as number) !== (payloads.length as number))) {
        return null;
    }
    let packets: Array<SceneMeshDrawPacket> = [];
    let i: number = 0;
    while ((i < resources.length)) {
        const mesh_resources: SceneMeshResources = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(resources, i);
        const transform_payload: SceneMeshTransformPayload = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(payloads, i);
        if (((scene_handle_equals(mesh_resources.node, transform_payload.node) as boolean) === (false as boolean))) {
            return null;
        }
        packets.push(Object.assign(new SceneMeshDrawPacket(), { node: Object.assign(new SceneHandle(), { index: mesh_resources.node.index, generation: mesh_resources.node.generation }), geometry: Object.assign(new ResourceHandle(), { index: mesh_resources.geometry.index, generation: mesh_resources.geometry.generation }), material: Object.assign(new ResourceHandle(), { index: mesh_resources.material.index, generation: mesh_resources.material.generation }), transform: transform_payload.payload }));
        i = (i + 1);
    }
    return packets;
}
function scene_visible_mesh_draw_packet_count(store: SceneStore, root: SceneHandle, view_projection: any): number | null {
    const packets_result: Array<SceneMeshDrawPacket> | null = scene_visible_mesh_draw_packets(store, root, view_projection);
    if ((packets_result === null)) {
        return null;
    }
    const empty_packets: Array<SceneMeshDrawPacket> = [];
    const packets: Array<SceneMeshDrawPacket> = (packets_result ?? empty_packets);
    let count: number = 0;
    for (const packet of packets) {
        count = (count + 1);
    }
    return count;
}
function scene_visible_mesh_draw_batch_facts(store: SceneStore, root: SceneHandle, view_projection: any): SceneVisibleMeshDrawBatchFacts | null {
    const packet_count_result: number | null = scene_visible_mesh_draw_packet_count(store, root, view_projection);
    if ((packet_count_result === null)) {
        return null;
    }
    const transform_bytes_result: number | null = scene_visible_mesh_transform_payload_byte_count(store, root, view_projection);
    if ((transform_bytes_result === null)) {
        return null;
    }
    return Object.assign(new SceneVisibleMeshDrawBatchFacts(), { packet_count: (packet_count_result ?? 0), transform_payload_byte_count: (transform_bytes_result ?? 0) });
}
function scene_effective_visible(store: SceneStore, handle: SceneHandle): boolean {
    let cursor: SceneHandle = Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation });
    let remaining: number = (store.slots.length + 1);
    while ((remaining > 0)) {
        const current: SceneNode | null = scene_get(store, cursor);
        if ((current === null)) {
            return false;
        }
        if (((current!.visible as boolean) === (false as boolean))) {
            return false;
        }
        if ((current!.parent === null)) {
            return true;
        }
        const parent: SceneHandle = Object.assign(new SceneHandle(), { index: current!.parent!.index, generation: current!.parent!.generation });
        cursor = Object.assign(new SceneHandle(), { index: parent.index, generation: parent.generation });
        remaining = (remaining - 1);
    }
    return false;
}
function scene_find_name(store: SceneStore, root: SceneHandle, name: string): SceneHandle | null {
    const result: SceneTraversal | null = _scene_traverse_preorder(store, root, [], store.slots.length);
    if ((result === null)) {
        return null;
    }
    const traversal: SceneTraversal = Object.assign(new SceneTraversal(), { handles: result!.handles, remaining: result!.remaining });
    let i: number = 0;
    while ((i < traversal.handles.length)) {
        const handle: SceneHandle = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(traversal.handles, i);
        const node: SceneNode | null = scene_get(store, handle);
        if ((node === null)) {
            return null;
        }
        if (((node!.name as string) === (name as string))) {
            return Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation });
        }
        i = (i + 1);
    }
    return null;
}
function _scene_replace(store: SceneStore, handle: SceneHandle, node: SceneNode): SceneStore {
    let slots: Array<SceneSlot> = [];
    let i: number = 0;
    while ((i < store.slots.length)) {
        const slot: SceneSlot = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(store.slots, i);
        if (((((i as number) === (handle.index as number)) && slot.alive) && ((slot.generation as number) === (handle.generation as number)))) {
            slots.push(Object.assign(new SceneSlot(), { generation: slot.generation, alive: true, node: node }));
        } else {
            slots.push(slot);
        }
        i = (i + 1);
    }
    return Object.assign(new SceneStore(), { slots: slots });
}
function _scene_without_child(parent: SceneNode, child: SceneHandle): SceneNode {
    let children: Array<SceneHandle> = [];
    let i: number = 0;
    while ((i < parent.children.length)) {
        const existing: SceneHandle = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(parent.children, i);
        if (((scene_handle_equals(existing, child) as boolean) === (false as boolean))) {
            children.push(Object.assign(new SceneHandle(), { index: existing.index, generation: existing.generation }));
        }
        i = (i + 1);
    }
    return Object.assign(new SceneNode(), { name: parent.name, kind: parent.kind, parent: parent.parent, children: children, visible: parent.visible, local_matrix: parent.local_matrix, world_matrix: parent.world_matrix, world_dirty: parent.world_dirty });
}
function _scene_would_cycle(store: SceneStore, child: SceneHandle, parent: SceneHandle): boolean {
    let cursor: SceneHandle = Object.assign(new SceneHandle(), { index: parent.index, generation: parent.generation });
    let remaining: number = (store.slots.length + 1);
    while ((remaining > 0)) {
        if (scene_handle_equals(cursor, child)) {
            return true;
        }
        const current: SceneNode | null = scene_get(store, cursor);
        if ((current === null)) {
            return false;
        }
        const current_node: SceneNode = Object.assign(new SceneNode(), { name: current!.name, kind: current!.kind, parent: current!.parent, children: current!.children, visible: current!.visible, local_matrix: current!.local_matrix, world_matrix: current!.world_matrix, world_dirty: current!.world_dirty });
        if ((current_node.parent === null)) {
            return false;
        }
        const current_parent: SceneHandle = Object.assign(new SceneHandle(), { index: current_node.parent!.index, generation: current_node.parent!.generation });
        cursor = Object.assign(new SceneHandle(), { index: current_parent.index, generation: current_parent.generation });
        remaining = (remaining - 1);
    }
    return true;
}
function scene_attach(store: SceneStore, child: SceneHandle, parent: SceneHandle): SceneStore | null {
    if (((scene_contains(store, child) as boolean) === (false as boolean))) {
        return null;
    }
    if (((scene_contains(store, parent) as boolean) === (false as boolean))) {
        return null;
    }
    if (_scene_would_cycle(store, child, parent)) {
        return null;
    }
    const child_result: SceneNode | null = scene_get(store, child);
    const parent_result: SceneNode | null = scene_get(store, parent);
    const child_node: SceneNode = Object.assign(new SceneNode(), { name: child_result!.name, kind: child_result!.kind, parent: child_result!.parent, children: child_result!.children, visible: child_result!.visible, local_matrix: child_result!.local_matrix, world_matrix: child_result!.world_matrix, world_dirty: child_result!.world_dirty });
    const parent_node: SceneNode = Object.assign(new SceneNode(), { name: parent_result!.name, kind: parent_result!.kind, parent: parent_result!.parent, children: parent_result!.children, visible: parent_result!.visible, local_matrix: parent_result!.local_matrix, world_matrix: parent_result!.world_matrix, world_dirty: parent_result!.world_dirty });
    if ((child_node.parent !== null)) {
        const existing_parent: SceneHandle = Object.assign(new SceneHandle(), { index: child_node.parent!.index, generation: child_node.parent!.generation });
        if (scene_handle_equals(existing_parent, parent)) {
            return null;
        }
    }
    let children: Array<SceneHandle> = [];
    let i: number = 0;
    while ((i < parent_node.children.length)) {
        const existing: SceneHandle = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(parent_node.children, i);
        children.push(Object.assign(new SceneHandle(), { index: existing.index, generation: existing.generation }));
        i = (i + 1);
    }
    children.push(Object.assign(new SceneHandle(), { index: child.index, generation: child.generation }));
    const updated_parent: SceneNode = Object.assign(new SceneNode(), { name: parent_node.name, kind: parent_node.kind, parent: parent_node.parent, children: children, visible: parent_node.visible, local_matrix: parent_node.local_matrix, world_matrix: parent_node.world_matrix, world_dirty: parent_node.world_dirty });
    const updated_child: SceneNode = Object.assign(new SceneNode(), { name: child_node.name, kind: child_node.kind, parent: Object.assign(new SceneHandle(), { index: parent.index, generation: parent.generation }), children: child_node.children, visible: child_node.visible, local_matrix: child_node.local_matrix, world_matrix: child_node.world_matrix, world_dirty: true });
    let without_old_parent: SceneStore = Object.assign(new SceneStore(), { slots: store.slots });
    if ((child_node.parent !== null)) {
        const old_parent_handle: SceneHandle = Object.assign(new SceneHandle(), { index: child_node.parent!.index, generation: child_node.parent!.generation });
        const old_parent_result: SceneNode | null = scene_get(store, old_parent_handle);
        if ((old_parent_result === null)) {
            return null;
        }
        const old_parent: SceneNode = Object.assign(new SceneNode(), { name: old_parent_result!.name, kind: old_parent_result!.kind, parent: old_parent_result!.parent, children: old_parent_result!.children, visible: old_parent_result!.visible, local_matrix: old_parent_result!.local_matrix, world_matrix: old_parent_result!.world_matrix, world_dirty: old_parent_result!.world_dirty });
        without_old_parent = _scene_replace(store, old_parent_handle, _scene_without_child(old_parent, child));
    }
    const with_parent: SceneStore = _scene_replace(without_old_parent, parent, updated_parent);
    const with_child: SceneStore = _scene_replace(with_parent, child, updated_child);
    return _scene_mark_world_dirty(with_child, child);
}
function scene_detach(store: SceneStore, child: SceneHandle): SceneStore | null {
    if (((scene_contains(store, child) as boolean) === (false as boolean))) {
        return null;
    }
    const child_result: SceneNode | null = scene_get(store, child);
    const child_node: SceneNode = Object.assign(new SceneNode(), { name: child_result!.name, kind: child_result!.kind, parent: child_result!.parent, children: child_result!.children, visible: child_result!.visible, local_matrix: child_result!.local_matrix, world_matrix: child_result!.world_matrix, world_dirty: child_result!.world_dirty });
    if ((child_node.parent === null)) {
        return null;
    }
    const parent_handle: SceneHandle = Object.assign(new SceneHandle(), { index: child_node.parent!.index, generation: child_node.parent!.generation });
    const parent_result: SceneNode | null = scene_get(store, parent_handle);
    if ((parent_result === null)) {
        return null;
    }
    const parent_node: SceneNode = Object.assign(new SceneNode(), { name: parent_result!.name, kind: parent_result!.kind, parent: parent_result!.parent, children: parent_result!.children, visible: parent_result!.visible, local_matrix: parent_result!.local_matrix, world_matrix: parent_result!.world_matrix, world_dirty: parent_result!.world_dirty });
    const detached_child: SceneNode = Object.assign(new SceneNode(), { name: child_node.name, kind: child_node.kind, children: child_node.children, visible: child_node.visible, local_matrix: child_node.local_matrix, world_matrix: child_node.world_matrix, world_dirty: true });
    const without_child: SceneStore = _scene_replace(store, parent_handle, _scene_without_child(parent_node, child));
    const with_child: SceneStore = _scene_replace(without_child, child, detached_child);
    return _scene_mark_world_dirty(with_child, child);
}
function _scene_mark_world_dirty(store: SceneStore, handle: SceneHandle): SceneStore | null {
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    let updated: SceneStore = _scene_replace(store, handle, Object.assign(new SceneNode(), { name: node.name, kind: node.kind, parent: node.parent, children: node.children, visible: node.visible, local_matrix: node.local_matrix, world_matrix: node.world_matrix, world_dirty: true }));
    let i: number = 0;
    while ((i < node.children.length)) {
        const current: SceneStore | null = _scene_mark_world_dirty(updated, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i));
        if ((current !== null)) {
            updated = Object.assign(new SceneStore(), { slots: current!.slots });
        } else {
            return null;
        }
        i = (i + 1);
    }
    return updated;
}
function scene_set_visible(store: SceneStore, handle: SceneHandle, visible: boolean): SceneStore | null {
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    return _scene_replace(store, handle, Object.assign(new SceneNode(), { name: node.name, kind: node.kind, parent: node.parent, children: node.children, visible: visible, local_matrix: node.local_matrix, world_matrix: node.world_matrix, world_dirty: node.world_dirty }));
}
function scene_set_local_matrix(store: SceneStore, handle: SceneHandle, local_matrix: any): SceneStore | null {
    if (((local_matrix.elements.length as number) !== (16 as number))) {
        return null;
    }
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    const with_local: SceneStore = _scene_replace(store, handle, Object.assign(new SceneNode(), { name: node.name, kind: node.kind, parent: node.parent, children: node.children, visible: node.visible, local_matrix: local_matrix, world_matrix: node.world_matrix, world_dirty: true }));
    return _scene_mark_world_dirty(with_local, handle);
}
function _scene_update_world_node(store: SceneStore, handle: SceneHandle, parent_world: any): SceneStore | null {
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node: SceneNode = Object.assign(new SceneNode(), { name: node_result!.name, kind: node_result!.kind, parent: node_result!.parent, children: node_result!.children, visible: node_result!.visible, local_matrix: node_result!.local_matrix, world_matrix: node_result!.world_matrix, world_dirty: node_result!.world_dirty });
    const world: any = triga.matrix4_multiplicata(parent_world, node.local_matrix);
    let updated: SceneStore = _scene_replace(store, handle, Object.assign(new SceneNode(), { name: node.name, kind: node.kind, parent: node.parent, children: node.children, visible: node.visible, local_matrix: node.local_matrix, world_matrix: world, world_dirty: false }));
    let i: number = 0;
    while ((i < node.children.length)) {
        const current: SceneStore | null = _scene_update_world_node(updated, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(node.children, i), world);
        if ((current !== null)) {
            updated = Object.assign(new SceneStore(), { slots: current!.slots });
        } else {
            return null;
        }
        i = (i + 1);
    }
    return updated;
}
function scene_update_world(store: SceneStore, root: SceneHandle): SceneStore | null {
    const root_result: SceneNode | null = scene_get(store, root);
    if ((root_result === null)) {
        return null;
    }
    if ((root_result!.parent !== null)) {
        return null;
    }
    return _scene_update_world_node(store, root, triga.matrix4_identitas());
}
function scene_remove(store: SceneStore, handle: SceneHandle): SceneStore | null {
    if (((scene_contains(store, handle) as boolean) === (false as boolean))) {
        return null;
    }
    const node_result: SceneNode | null = scene_get(store, handle);
    if ((node_result!.children.length > 0)) {
        return null;
    }
    let detached_store: SceneStore = Object.assign(new SceneStore(), { slots: store.slots });
    if ((node_result!.parent !== null)) {
        const current: SceneStore | null = scene_detach(store, handle);
        if ((current !== null)) {
            detached_store = Object.assign(new SceneStore(), { slots: current!.slots });
        } else {
            return null;
        }
    }
    let slots: Array<SceneSlot> = [];
    let i: number = 0;
    while ((i < detached_store.slots.length)) {
        const slot: SceneSlot = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(detached_store.slots, i);
        if (((i as number) === (handle.index as number))) {
            slots.push(Object.assign(new SceneSlot(), { generation: (slot.generation + 1), alive: false, node: slot.node }));
        } else {
            slots.push(slot);
        }
        i = (i + 1);
    }
    return Object.assign(new SceneStore(), { slots: slots });
}

export const scene = {
  ResourceHandle,
  ResourceHandleTraversal,
  ResourceLifecycleBatchFacts,
  ResourceLifecycleTransition,
  ResourceTransition,
  SceneCamera,
  SceneCount,
  SceneGroup,
  SceneHandle,
  SceneInsert,
  SceneLight,
  SceneMesh,
  SceneMeshDrawPacket,
  SceneMeshResourceTraversal,
  SceneMeshResources,
  SceneMeshTransformPayload,
  SceneNode,
  SceneSlot,
  SceneStore,
  SceneTraversal,
  SceneVisibleMeshDrawBatchFacts,
  SceneVisibleMeshResourceFacts,
  _resource_lifecycle_index_seen,
  _resource_transition_index_seen,
  _scene_count_visible_mesh_preorder,
  _scene_mark_world_dirty,
  _scene_replace,
  _scene_traversal_contains,
  _scene_traverse_preorder,
  _scene_traverse_visible_mesh_preorder,
  _scene_traverse_visible_mesh_resources_preorder,
  _scene_traverse_visible_preorder,
  _scene_update_world_node,
  _scene_visible_mesh_resource_handles_preorder,
  _scene_without_child,
  _scene_would_cycle,
  resource_handle_equals,
  resource_handle_next,
  resource_lifecycle_batch_facts,
  resource_lifecycle_changed_count,
  resource_lifecycle_changed_handles,
  resource_lifecycle_changed_indices,
  resource_lifecycle_created,
  resource_lifecycle_created_count,
  resource_lifecycle_current_handles,
  resource_lifecycle_live_count,
  resource_lifecycle_live_indices,
  resource_lifecycle_removed,
  resource_lifecycle_removed_count,
  resource_lifecycle_removed_handles,
  resource_lifecycle_removed_indices,
  resource_lifecycle_replaced,
  resource_lifecycle_retired_handles,
  resource_lifecycle_retired_indices,
  resource_lifecycle_unchanged,
  resource_lifecycle_unchanged_count,
  resource_lifecycle_unchanged_handles,
  resource_lifecycle_unchanged_indices,
  resource_lifecycle_valid,
  resource_lifecycles_valid,
  resource_transition_changed_count,
  resource_transition_changed_handles,
  resource_transition_changed_indices,
  resource_transition_current_handles,
  resource_transition_replaced,
  resource_transition_unchanged,
  resource_transition_unchanged_count,
  resource_transition_unchanged_handles,
  resource_transition_unchanged_indices,
  resource_transition_valid,
  resource_transitions_valid,
  scene_attach,
  scene_camera,
  scene_contains,
  scene_detach,
  scene_effective_visible,
  scene_find_name,
  scene_get,
  scene_group,
  scene_handle_equals,
  scene_insert,
  scene_light,
  scene_mesh,
  scene_mesh_geometry,
  scene_mesh_material,
  scene_mesh_world_matrix,
  scene_node,
  scene_remove,
  scene_set_local_matrix,
  scene_set_visible,
  scene_store,
  scene_traverse,
  scene_update_world,
  scene_visible_mesh_count,
  scene_visible_mesh_draw_batch_facts,
  scene_visible_mesh_draw_packet_count,
  scene_visible_mesh_draw_packets,
  scene_visible_mesh_geometry_handles,
  scene_visible_mesh_material_handles,
  scene_visible_mesh_resource_facts,
  scene_visible_mesh_resource_pair_count,
  scene_visible_mesh_resources,
  scene_visible_mesh_transform_payload_byte_count,
  scene_visible_mesh_transform_payload_count,
  scene_visible_mesh_transform_payloads,
  scene_visible_mesh_traverse,
  scene_visible_traverse,
};
