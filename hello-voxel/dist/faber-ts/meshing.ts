// @ts-nocheck
// Generated by radix - do not edit

import { voxel } from "./voxel.js";

import { triga } from "./triga-triga.js";

import { geometry } from "./triga-geometry.js";

class ChunkMesh {
    cx!: number;
    cz!: number;
    face_count!: number;
    positions!: Array<number>;
    colors!: Array<number>;
    indices!: Array<number>;
}

function neighbor_is_solid(world: any, x: number, y: number, z: number): boolean {
    const value: number | null = voxel.world_get(world, x, y, z);
    if ((value === null)) {
        return false;
    }
    return voxel.block_is_solid((value ?? 0));
}
function face_neighbor_world(x: number, y: number, z: number, face_code: number): any | null {
    if (((triga.face_code_valid(face_code) as boolean) === (false as boolean))) {
        return null;
    }
    const dx: number | null = triga.face_code_x_offset(face_code);
    const dy: number | null = triga.face_code_y_offset(face_code);
    const dz: number | null = triga.face_code_z_offset(face_code);
    if ((((dx === null) || (dy === null)) || (dz === null))) {
        return null;
    }
    return Object.assign({}, { x: (x + (dx ?? 0)), y: (y + (dy ?? 0)), z: (z + (dz ?? 0)) });
}
function face_is_visible(world: any, x: number, y: number, z: number, face_code: number): boolean {
    const cell: number | null = voxel.world_get(world, x, y, z);
    if ((cell === null)) {
        return false;
    }
    if (((voxel.block_is_solid((cell ?? 0)) as boolean) === (false as boolean))) {
        return false;
    }
    const neighbor: any | null = face_neighbor_world(x, y, z, face_code);
    if ((neighbor === null)) {
        return false;
    }
    return ((neighbor_is_solid(world, neighbor!.x, neighbor!.y, neighbor!.z) as boolean) === (false as boolean));
}
function empty_chunk_mesh(cx: number, cz: number): ChunkMesh {
    return Object.assign(new ChunkMesh(), { cx: cx, cz: cz, face_count: 0, positions: [], colors: [], indices: [] });
}
function append_visible_face(positions: Array<number>, colors: Array<number>, indices: Array<number>, face_code: number, x: number, y: number, z: number): any | null {
    return triga.face_code_colored_quad_mesh_append(positions, colors, indices, face_code, x, y, z, 1 as number);
}
function mesh_chunk(world: any, cx: number, cz: number): ChunkMesh | null {
    if (((voxel.world_valid(world) as boolean) === (false as boolean))) {
        return null;
    }
    if (((voxel.chunk_coord_valid(cx, cz) as boolean) === (false as boolean))) {
        return null;
    }
    let positions: Array<number> = [];
    let colors: Array<number> = [];
    let indices: Array<number> = [];
    let face_count: number = 0;
    const base_x: number = (cx * voxel.chunk_size());
    const base_z: number = (cz * voxel.chunk_size());
    let ly: number = 0;
    while ((ly < voxel.chunk_size())) {
        let lz: number = 0;
        while ((lz < voxel.chunk_size())) {
            let lx: number = 0;
            while ((lx < voxel.chunk_size())) {
                const wx: number = (base_x + lx);
                const wy: number = ly;
                const wz: number = (base_z + lz);
                const cell: number | null = voxel.world_get(world, wx, wy, wz);
                if ((cell !== null)) {
                    if (voxel.block_is_solid((cell ?? 0))) {
                        let face_code: number = 1;
                        while ((face_code <= 6)) {
                            if (face_is_visible(world, wx, wy, wz, face_code)) {
                                const appended: any | null = append_visible_face(positions, colors, indices, face_code, wx, wy, wz);
                                if ((appended === null)) {
                                    return null;
                                }
                                positions = appended!.positions;
                                colors = appended!.colors;
                                indices = appended!.indices;
                                face_count = (face_count + 1);
                            }
                            face_code = (face_code + 1);
                        }
                    }
                }
                lx = (lx + 1);
            }
            lz = (lz + 1);
        }
        ly = (ly + 1);
    }
    if (((face_count as number) === (0 as number))) {
        return empty_chunk_mesh(cx, cz);
    }
    return Object.assign(new ChunkMesh(), { cx: cx, cz: cz, face_count: face_count, positions: positions, colors: colors, indices: indices });
}
function chunk_mesh_colored(mesh: ChunkMesh): any {
    return Object.assign({}, { positions: mesh.positions, colors: mesh.colors, indices: mesh.indices });
}
function chunk_mesh_facts(mesh: ChunkMesh): any | null {
    if (((mesh.face_count as number) === (0 as number))) {
        return null;
    }
    return geometry.colored_quad_mesh_facts(chunk_mesh_colored(mesh));
}
function chunk_mesh_bounding_box(mesh: ChunkMesh): any | null {
    if (((mesh.face_count as number) === (0 as number))) {
        return null;
    }
    return geometry.colored_quad_mesh_bounding_box(chunk_mesh_colored(mesh));
}
function chunk_mesh_matches_face_count(mesh: ChunkMesh): boolean {
    if (((mesh.face_count as number) === (0 as number))) {
        return ((((mesh.positions.length as number) === (0 as number)) && ((mesh.colors.length as number) === (0 as number))) && ((mesh.indices.length as number) === (0 as number)));
    }
    const facts: any | null = chunk_mesh_facts(mesh);
    if ((facts === null)) {
        return false;
    }
    return ((((((((facts!.face_count as number) === (mesh.face_count as number)) && ((facts!.vertex_count as number) === ((mesh.face_count * 4) as number))) && ((facts!.index_count as number) === ((mesh.face_count * 6) as number))) && ((facts!.triangle_count as number) === ((mesh.face_count * 2) as number))) && ((mesh.positions.length as number) === ((mesh.face_count * 12) as number))) && ((mesh.colors.length as number) === ((mesh.face_count * 12) as number))) && ((mesh.indices.length as number) === ((mesh.face_count * 6) as number)));
}
function chunk_mesh_count_face_code(mesh: ChunkMesh, face_code: number): number | null {
    if (((triga.face_code_valid(face_code) as boolean) === (false as boolean))) {
        return null;
    }
    const expected: any | null = triga.face_code_color(face_code);
    if ((expected === null)) {
        return null;
    }
    let count: number = 0;
    let face: number = 0;
    while ((face < mesh.face_count)) {
        const base: number = (face * 12);
        if (((((((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.colors, base) as number) === (expected!.r as number)) && ((((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.colors, (base + 1)) as number) === (expected!.g as number))) && ((((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.colors, (base + 2)) as number) === (expected!.b as number)))) {
            count = (count + 1);
        }
        face = (face + 1);
    }
    return count;
}
function chunk_mesh_face_winding_matches(mesh: ChunkMesh, face_index: number, face_code: number): boolean {
    if (((face_index < 0) || (face_index >= mesh.face_count))) {
        return false;
    }
    const expected_normal: any | null = triga.face_code_normal(face_code);
    if ((expected_normal === null)) {
        return false;
    }
    const index_base: number = (face_index * 6);
    const i0: number = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.indices, index_base);
    const i1: number = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.indices, (index_base + 1));
    const i2: number = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.indices, (index_base + 2));
    const p0: number = (i0 * 3);
    const p1: number = (i1 * 3);
    const p2: number = (i2 * 3);
    const a: any = triga.vector3(((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.positions, p0), ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.positions, (p0 + 1)), ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.positions, (p0 + 2)));
    const b: any = triga.vector3(((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.positions, p1), ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.positions, (p1 + 1)), ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.positions, (p1 + 2)));
    const c: any = triga.vector3(((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.positions, p2), ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.positions, (p2 + 1)), ((__o, __i) => { const __v = __o[__i]; if (__v === undefined) throw new Error("index trap"); return __v; })(mesh.positions, (p2 + 2)));
    const edge_ab: any = triga.vector3_subtracta(b, a);
    const edge_ac: any = triga.vector3_subtracta(c, a);
    const winding: any = triga.vector3_normalizata(triga.vector3_cross(edge_ab, edge_ac));
    return ((((winding.x as number) === (expected_normal!.x as number)) && ((winding.y as number) === (expected_normal!.y as number))) && ((winding.z as number) === (expected_normal!.z as number)));
}

export const meshing = {
  ChunkMesh,
  append_visible_face,
  chunk_mesh_bounding_box,
  chunk_mesh_colored,
  chunk_mesh_count_face_code,
  chunk_mesh_face_winding_matches,
  chunk_mesh_facts,
  chunk_mesh_matches_face_count,
  empty_chunk_mesh,
  face_is_visible,
  face_neighbor_world,
  mesh_chunk,
  neighbor_is_solid,
};
