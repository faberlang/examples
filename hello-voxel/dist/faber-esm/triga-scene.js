// @ts-nocheck
// Generated by radix - do not edit
import { triga } from "./triga-triga.js";
class SceneHandle {
    index;
    generation;
}
class ResourceHandle {
    index;
    generation;
}
class ResourceTransition {
    previous;
    current;
    changed;
}
class ResourceLifecycleTransition {
    logical_index;
    previous;
    current;
    changed;
    removed;
}
class ResourceLifecycleBatchFacts {
    transition_count;
    changed_count;
    unchanged_count;
    created_count;
    removed_count;
    live_count;
}
const SceneGroup = { tag: "SceneGroup" };
function SceneMesh(geometry, material) { return { tag: "SceneMesh", geometry, material }; }
const SceneCamera = { tag: "SceneCamera" };
const SceneLight = { tag: "SceneLight" };
class SceneNode {
    name;
    kind;
    parent;
    children;
    visible;
    local_matrix;
    world_matrix;
    world_dirty;
}
class SceneSlot {
    generation;
    alive;
    node;
}
class SceneStore {
    slots;
}
class SceneInsert {
    store;
    handle;
}
class SceneTraversal {
    handles;
    remaining;
}
class SceneCount {
    count;
    remaining;
}
class SceneMeshResources {
    node;
    geometry;
    material;
}
class SceneMeshTransformPayload {
    node;
    payload;
}
class SceneMeshDrawPacket {
    node;
    geometry;
    material;
    transform;
}
class SceneVisibleMeshDrawBatchFacts {
    packet_count;
    transform_payload_byte_count;
}
class SceneVisibleMeshResourceFacts {
    mesh_count;
    resource_pair_count;
    geometry_handle_count;
    material_handle_count;
}
class SceneMeshResourceTraversal {
    resources;
    remaining;
}
class ResourceHandleTraversal {
    handles;
    remaining;
}
function scene_handle_equals(a, b) {
    return ((a.index === b.index) && (a.generation === b.generation));
}
function resource_handle_equals(a, b) {
    return ((a.index === b.index) && (a.generation === b.generation));
}
function resource_handle_next(handle) {
    return Object.assign(new ResourceHandle(), { index: handle.index, generation: (handle.generation + 1) });
}
function resource_transition_unchanged(handle) {
    const stable = Object.assign(new ResourceHandle(), { index: handle.index, generation: handle.generation });
    return Object.assign(new ResourceTransition(), { previous: stable, current: stable, changed: false });
}
function resource_transition_replaced(handle) {
    const previous = Object.assign(new ResourceHandle(), { index: handle.index, generation: handle.generation });
    return Object.assign(new ResourceTransition(), { previous: previous, current: resource_handle_next(previous), changed: true });
}
function resource_transition_valid(transition) {
    if ((transition.previous.index !== transition.current.index)) {
        return false;
    }
    if (transition.changed) {
        return (transition.current.generation === (transition.previous.generation + 1));
    }
    return (transition.current.generation === transition.previous.generation);
}
function _resource_transition_index_seen(transitions, limit, resource_index) {
    let i = 0;
    while ((i < limit)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if ((transition.current.index === resource_index)) {
            return true;
        }
        i = (i + 1);
    }
    return false;
}
function resource_transitions_valid(transitions) {
    let i = 0;
    while ((i < transitions.length)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if ((resource_transition_valid(transition) === false)) {
            return false;
        }
        if (_resource_transition_index_seen(transitions, i, transition.current.index)) {
            return false;
        }
        i = (i + 1);
    }
    return true;
}
function resource_transition_changed_count(transitions) {
    if ((resource_transitions_valid(transitions) === false)) {
        return null;
    }
    let count = 0;
    let i = 0;
    while ((i < transitions.length)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if (transition.changed) {
            count = (count + 1);
        }
        i = (i + 1);
    }
    return count;
}
function resource_transition_unchanged_count(transitions) {
    if ((resource_transitions_valid(transitions) === false)) {
        return null;
    }
    let count = 0;
    let i = 0;
    while ((i < transitions.length)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if ((transition.changed === false)) {
            count = (count + 1);
        }
        i = (i + 1);
    }
    return count;
}
function resource_transition_changed_indices(transitions) {
    if ((resource_transitions_valid(transitions) === false)) {
        return null;
    }
    let indices = [];
    let i = 0;
    while ((i < transitions.length)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if (transition.changed) {
            indices.push(transition.current.index);
        }
        i = (i + 1);
    }
    return indices;
}
function resource_transition_changed_handles(transitions) {
    if ((resource_transitions_valid(transitions) === false)) {
        return null;
    }
    let handles = [];
    let i = 0;
    while ((i < transitions.length)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if (transition.changed) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current.index, generation: transition.current.generation }));
        }
        i = (i + 1);
    }
    return handles;
}
function resource_transition_current_handles(transitions) {
    if ((resource_transitions_valid(transitions) === false)) {
        return null;
    }
    let handles = [];
    let i = 0;
    while ((i < transitions.length)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        handles.push(Object.assign(new ResourceHandle(), { index: transition.current.index, generation: transition.current.generation }));
        i = (i + 1);
    }
    return handles;
}
function resource_transition_unchanged_indices(transitions) {
    if ((resource_transitions_valid(transitions) === false)) {
        return null;
    }
    let indices = [];
    let i = 0;
    while ((i < transitions.length)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if ((transition.changed === false)) {
            indices.push(transition.current.index);
        }
        i = (i + 1);
    }
    return indices;
}
function resource_transition_unchanged_handles(transitions) {
    if ((resource_transitions_valid(transitions) === false)) {
        return null;
    }
    let handles = [];
    let i = 0;
    while ((i < transitions.length)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if ((transition.changed === false)) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current.index, generation: transition.current.generation }));
        }
        i = (i + 1);
    }
    return handles;
}
function resource_lifecycle_unchanged(handle) {
    return Object.assign(new ResourceLifecycleTransition(), { logical_index: handle.index, previous: handle, current: handle, changed: false, removed: false });
}
function resource_lifecycle_replaced(handle) {
    return Object.assign(new ResourceLifecycleTransition(), { logical_index: handle.index, previous: handle, current: resource_handle_next(handle), changed: true, removed: false });
}
function resource_lifecycle_removed(handle) {
    return Object.assign(new ResourceLifecycleTransition(), { logical_index: handle.index, previous: handle, current: null, changed: true, removed: true });
}
function resource_lifecycle_created(handle) {
    return Object.assign(new ResourceLifecycleTransition(), { logical_index: handle.index, previous: null, current: handle, changed: true, removed: false });
}
function resource_lifecycle_valid(transition) {
    if (((transition.previous === null) && (transition.current === null))) {
        return false;
    }
    if ((transition.previous !== null)) {
        if ((transition.previous.index !== transition.logical_index)) {
            return false;
        }
    }
    if ((transition.current !== null)) {
        if ((transition.current.index !== transition.logical_index)) {
            return false;
        }
    }
    if (transition.removed) {
        return ((transition.changed && (transition.previous !== null)) && (transition.current === null));
    }
    if ((transition.changed === false)) {
        return (((transition.previous !== null) && (transition.current !== null)) && (transition.previous.generation === transition.current.generation));
    }
    if ((transition.previous === null)) {
        return (transition.current !== null);
    }
    if ((transition.current === null)) {
        return false;
    }
    return (transition.current.generation === (transition.previous.generation + 1));
}
function _resource_lifecycle_index_seen(transitions, limit, logical_index) {
    let i = 0;
    while ((i < limit)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if ((transition.logical_index === logical_index)) {
            return true;
        }
        i = (i + 1);
    }
    return false;
}
function resource_lifecycles_valid(transitions) {
    let i = 0;
    while ((i < transitions.length)) {
        const transition = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(transitions, i);
        if ((resource_lifecycle_valid(transition) === false)) {
            return false;
        }
        if (_resource_lifecycle_index_seen(transitions, i, transition.logical_index)) {
            return false;
        }
        i = (i + 1);
    }
    return true;
}
function resource_lifecycle_changed_count(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let count = 0;
    for (const transition of transitions) {
        if (transition.changed) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_unchanged_count(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let count = 0;
    for (const transition of transitions) {
        if ((transition.changed === false)) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_created_count(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let count = 0;
    for (const transition of transitions) {
        if (((transition.previous === null) && (transition.current !== null))) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_removed_count(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let count = 0;
    for (const transition of transitions) {
        if (transition.removed) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_live_count(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let count = 0;
    for (const transition of transitions) {
        if ((transition.current !== null)) {
            count = (count + 1);
        }
    }
    return count;
}
function resource_lifecycle_batch_facts(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let changed_count = 0;
    let unchanged_count = 0;
    let created_count = 0;
    let removed_count = 0;
    let live_count = 0;
    for (const transition of transitions) {
        if (transition.changed) {
            changed_count = (changed_count + 1);
        }
        if ((transition.changed === false)) {
            unchanged_count = (unchanged_count + 1);
        }
        if (((transition.previous === null) && (transition.current !== null))) {
            created_count = (created_count + 1);
        }
        if (transition.removed) {
            removed_count = (removed_count + 1);
        }
        if ((transition.current !== null)) {
            live_count = (live_count + 1);
        }
    }
    return Object.assign(new ResourceLifecycleBatchFacts(), { transition_count: transitions.length, changed_count: changed_count, unchanged_count: unchanged_count, created_count: created_count, removed_count: removed_count, live_count: live_count });
}
function resource_lifecycle_changed_indices(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let indices = [];
    for (const transition of transitions) {
        if (transition.changed) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_unchanged_indices(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let indices = [];
    for (const transition of transitions) {
        if ((transition.changed === false)) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_removed_indices(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let indices = [];
    for (const transition of transitions) {
        if (transition.removed) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_live_indices(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let indices = [];
    for (const transition of transitions) {
        if ((transition.current !== null)) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_retired_indices(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let indices = [];
    for (const transition of transitions) {
        if ((transition.changed && (transition.previous !== null))) {
            indices.push(transition.logical_index);
        }
    }
    return indices;
}
function resource_lifecycle_changed_handles(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let handles = [];
    for (const transition of transitions) {
        if ((transition.changed && (transition.current !== null))) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current.index, generation: transition.current.generation }));
        }
    }
    return handles;
}
function resource_lifecycle_unchanged_handles(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let handles = [];
    for (const transition of transitions) {
        if (((transition.changed === false) && (transition.current !== null))) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current.index, generation: transition.current.generation }));
        }
    }
    return handles;
}
function resource_lifecycle_removed_handles(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let handles = [];
    for (const transition of transitions) {
        if ((transition.removed && (transition.previous !== null))) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.previous.index, generation: transition.previous.generation }));
        }
    }
    return handles;
}
function resource_lifecycle_retired_handles(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let handles = [];
    for (const transition of transitions) {
        if ((transition.changed && (transition.previous !== null))) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.previous.index, generation: transition.previous.generation }));
        }
    }
    return handles;
}
function resource_lifecycle_current_handles(transitions) {
    if ((resource_lifecycles_valid(transitions) === false)) {
        return null;
    }
    let handles = [];
    for (const transition of transitions) {
        if ((transition.current !== null)) {
            handles.push(Object.assign(new ResourceHandle(), { index: transition.current.index, generation: transition.current.generation }));
        }
    }
    return handles;
}
function scene_store() {
    return Object.assign(new SceneStore(), { slots: [] });
}
function scene_node(name, kind) {
    return Object.assign(new SceneNode(), { name: name, kind: kind, children: [], visible: true, local_matrix: triga.matrix4_identitas(), world_matrix: triga.matrix4_identitas(), world_dirty: true });
}
function scene_group(name) {
    return scene_node(name, SceneGroup);
}
function scene_mesh(name, geometry, material) {
    return scene_node(name, SceneMesh(Object.assign(new ResourceHandle(), { index: geometry.index, generation: geometry.generation }), Object.assign(new ResourceHandle(), { index: material.index, generation: material.generation })));
}
function scene_camera(name) {
    return scene_node(name, SceneCamera);
}
function scene_light(name) {
    return scene_node(name, SceneLight);
}
function scene_mesh_geometry(node) {
    {
        let t1365 = ((value) => value)(node.kind);
        if (t1365.tag === "SceneMesh") {
            const geometry = t1365.geometry;
            const material = t1365.material;
            return geometry;
        }
        else if (t1365.tag === "SceneGroup") {
            return null;
        }
        else if (t1365.tag === "SceneCamera") {
            return null;
        }
        else if (t1365.tag === "SceneLight") {
            return null;
        }
        else {
            throw new Error("non-exhaustive discerne");
        }
    }
}
function scene_mesh_material(node) {
    {
        let t1382 = ((value) => value)(node.kind);
        if (t1382.tag === "SceneMesh") {
            const geometry = t1382.geometry;
            const material = t1382.material;
            return material;
        }
        else if (t1382.tag === "SceneGroup") {
            return null;
        }
        else if (t1382.tag === "SceneCamera") {
            return null;
        }
        else if (t1382.tag === "SceneLight") {
            return null;
        }
        else {
            throw new Error("non-exhaustive discerne");
        }
    }
}
function scene_mesh_world_matrix(store, handle) {
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    {
        let t1412 = ((value) => value)(node_result.kind);
        if (t1412.tag === "SceneMesh") {
            const geometry = t1412.geometry;
            const material = t1412.material;
            return node_result.world_matrix;
        }
        else if (t1412.tag === "SceneGroup") {
            return null;
        }
        else if (t1412.tag === "SceneCamera") {
            return null;
        }
        else if (t1412.tag === "SceneLight") {
            return null;
        }
        else {
            throw new Error("non-exhaustive discerne");
        }
    }
}
function scene_insert(store, node) {
    let slots = [];
    let i = 0;
    while ((i < store.slots.length)) {
        slots.push(((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(store.slots, i));
        i = (i + 1);
    }
    const index = slots.length;
    slots.push(Object.assign(new SceneSlot(), { generation: 0, alive: true, node: node }));
    return Object.assign(new SceneInsert(), { store: Object.assign(new SceneStore(), { slots: slots }), handle: Object.assign(new SceneHandle(), { index: index, generation: 0 }) });
}
function scene_contains(store, handle) {
    if ((handle.index >= store.slots.length)) {
        return false;
    }
    const slot = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
        throw new Error("index trap"); return __v; })(store.slots, handle.index);
    return (slot.alive && (slot.generation === handle.generation));
}
function scene_get(store, handle) {
    if ((scene_contains(store, handle) === false)) {
        return null;
    }
    return ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
        throw new Error("index trap"); return __v; })(store.slots, handle.index).node;
}
function _scene_traversal_contains(handles, handle) {
    let i = 0;
    while ((i < handles.length)) {
        const existing = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(handles, i);
        if ((scene_handle_equals(existing, handle) === true)) {
            return true;
        }
        i = (i + 1);
    }
    return false;
}
function _scene_traverse_preorder(store, handle, handles, remaining) {
    if ((remaining === 0)) {
        return null;
    }
    if (_scene_traversal_contains(handles, handle)) {
        return null;
    }
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    let updated_handles = handles;
    updated_handles.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    let traversal = Object.assign(new SceneTraversal(), { handles: updated_handles, remaining: (remaining - 1) });
    let i = 0;
    while ((i < node.children.length)) {
        const current = _scene_traverse_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(node.children, i), traversal.handles, traversal.remaining);
        if ((current === null)) {
            return null;
        }
        const current_value = Object.assign(new SceneTraversal(), { handles: current.handles, remaining: current.remaining });
        traversal = Object.assign(new SceneTraversal(), { handles: current_value.handles, remaining: current_value.remaining });
        i = (i + 1);
    }
    return traversal;
}
function scene_traverse(store, root) {
    const traversal = _scene_traverse_preorder(store, root, [], store.slots.length);
    if ((traversal === null)) {
        return null;
    }
    return traversal.handles;
}
function _scene_traverse_visible_preorder(store, handle, handles, remaining, parent_visible) {
    if ((remaining === 0)) {
        return null;
    }
    if (_scene_traversal_contains(handles, handle)) {
        return null;
    }
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    const effective_visible = (parent_visible && node.visible);
    let updated_handles = handles;
    if (effective_visible) {
        updated_handles.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    }
    let traversal = Object.assign(new SceneTraversal(), { handles: updated_handles, remaining: (remaining - 1) });
    let i = 0;
    while ((i < node.children.length)) {
        const current = _scene_traverse_visible_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(node.children, i), traversal.handles, traversal.remaining, effective_visible);
        if ((current === null)) {
            return null;
        }
        const current_value = Object.assign(new SceneTraversal(), { handles: current.handles, remaining: current.remaining });
        traversal = Object.assign(new SceneTraversal(), { handles: current_value.handles, remaining: current_value.remaining });
        i = (i + 1);
    }
    return traversal;
}
function scene_visible_traverse(store, root) {
    const traversal = _scene_traverse_visible_preorder(store, root, [], store.slots.length, true);
    if ((traversal === null)) {
        return null;
    }
    return traversal.handles;
}
function _scene_traverse_visible_mesh_preorder(store, handle, handles, remaining, parent_visible) {
    if ((remaining === 0)) {
        return null;
    }
    if (_scene_traversal_contains(handles, handle)) {
        return null;
    }
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    const effective_visible = (parent_visible && node.visible);
    let updated_handles = handles;
    if (effective_visible) {
        {
            let t1890 = ((value) => value)(node.kind);
            if (t1890.tag === "SceneMesh") {
                const geometry = t1890.geometry;
                const material = t1890.material;
                updated_handles.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
            }
            else if (t1890.tag === "SceneGroup") {
            }
            else if (t1890.tag === "SceneCamera") {
            }
            else if (t1890.tag === "SceneLight") {
            }
            else {
                throw new Error("non-exhaustive discerne");
            }
        }
    }
    let traversal = Object.assign(new SceneTraversal(), { handles: updated_handles, remaining: (remaining - 1) });
    let i = 0;
    while ((i < node.children.length)) {
        const current = _scene_traverse_visible_mesh_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(node.children, i), traversal.handles, traversal.remaining, effective_visible);
        if ((current === null)) {
            return null;
        }
        const current_value = Object.assign(new SceneTraversal(), { handles: current.handles, remaining: current.remaining });
        traversal = Object.assign(new SceneTraversal(), { handles: current_value.handles, remaining: current_value.remaining });
        i = (i + 1);
    }
    return traversal;
}
function scene_visible_mesh_traverse(store, root) {
    const traversal = _scene_traverse_visible_mesh_preorder(store, root, [], store.slots.length, true);
    if ((traversal === null)) {
        return null;
    }
    return traversal.handles;
}
function _scene_count_visible_mesh_preorder(store, handle, seen, remaining, parent_visible) {
    if ((remaining === 0)) {
        return null;
    }
    if (_scene_traversal_contains(seen, handle)) {
        return null;
    }
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    const effective_visible = (parent_visible && node.visible);
    let updated_seen = seen;
    updated_seen.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    let count = 0;
    if (effective_visible) {
        {
            let t2051 = ((value) => value)(node.kind);
            if (t2051.tag === "SceneMesh") {
                const geometry = t2051.geometry;
                const material = t2051.material;
                count = 1;
            }
            else if (t2051.tag === "SceneGroup") {
            }
            else if (t2051.tag === "SceneCamera") {
            }
            else if (t2051.tag === "SceneLight") {
            }
            else {
                throw new Error("non-exhaustive discerne");
            }
        }
    }
    let result = Object.assign(new SceneCount(), { count: count, remaining: (remaining - 1) });
    let i = 0;
    while ((i < node.children.length)) {
        const child_count = _scene_count_visible_mesh_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(node.children, i), updated_seen, result.remaining, effective_visible);
        if ((child_count === null)) {
            return null;
        }
        const child_value = Object.assign(new SceneCount(), { count: child_count.count, remaining: child_count.remaining });
        result = Object.assign(new SceneCount(), { count: (result.count + child_value.count), remaining: child_value.remaining });
        updated_seen.push(Object.assign(new SceneHandle(), { index: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
                throw new Error("index trap"); return __v; })(node.children, i).index, generation: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
                throw new Error("index trap"); return __v; })(node.children, i).generation }));
        i = (i + 1);
    }
    return result;
}
function scene_visible_mesh_count(store, root) {
    const count_result = _scene_count_visible_mesh_preorder(store, root, [], store.slots.length, true);
    if ((count_result === null)) {
        return null;
    }
    return count_result.count;
}
function _scene_traverse_visible_mesh_resources_preorder(store, handle, resources, seen, remaining, parent_visible) {
    if ((remaining === 0)) {
        return null;
    }
    if (_scene_traversal_contains(seen, handle)) {
        return null;
    }
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    const effective_visible = (parent_visible && node.visible);
    let updated_seen = seen;
    updated_seen.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    let updated_resources = resources;
    if (effective_visible) {
        {
            let t2248 = ((value) => value)(node.kind);
            if (t2248.tag === "SceneMesh") {
                const geometry = t2248.geometry;
                const material = t2248.material;
                updated_resources.push(Object.assign(new SceneMeshResources(), { node: Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }), geometry: Object.assign(new ResourceHandle(), { index: geometry.index, generation: geometry.generation }), material: Object.assign(new ResourceHandle(), { index: material.index, generation: material.generation }) }));
            }
            else if (t2248.tag === "SceneGroup") {
            }
            else if (t2248.tag === "SceneCamera") {
            }
            else if (t2248.tag === "SceneLight") {
            }
            else {
                throw new Error("non-exhaustive discerne");
            }
        }
    }
    let traversal = Object.assign(new SceneMeshResourceTraversal(), { resources: updated_resources, remaining: (remaining - 1) });
    let i = 0;
    while ((i < node.children.length)) {
        const current = _scene_traverse_visible_mesh_resources_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(node.children, i), traversal.resources, updated_seen, traversal.remaining, effective_visible);
        if ((current === null)) {
            return null;
        }
        const current_value = Object.assign(new SceneMeshResourceTraversal(), { resources: current.resources, remaining: current.remaining });
        traversal = Object.assign(new SceneMeshResourceTraversal(), { resources: current_value.resources, remaining: current_value.remaining });
        updated_seen.push(Object.assign(new SceneHandle(), { index: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
                throw new Error("index trap"); return __v; })(node.children, i).index, generation: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
                throw new Error("index trap"); return __v; })(node.children, i).generation }));
        i = (i + 1);
    }
    return traversal;
}
function scene_visible_mesh_resources(store, root) {
    const traversal = _scene_traverse_visible_mesh_resources_preorder(store, root, [], [], store.slots.length, true);
    if ((traversal === null)) {
        return null;
    }
    return traversal.resources;
}
function scene_visible_mesh_resource_pair_count(store, root) {
    return scene_visible_mesh_count(store, root);
}
function _scene_visible_mesh_resource_handles_preorder(store, handle, handles, seen, remaining, parent_visible, geometry_handles) {
    if ((remaining === 0)) {
        return null;
    }
    if (_scene_traversal_contains(seen, handle)) {
        return null;
    }
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    const effective_visible = (parent_visible && node.visible);
    let updated_seen = seen;
    updated_seen.push(Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }));
    let updated_handles = handles;
    if (effective_visible) {
        {
            let t2449 = ((value) => value)(node.kind);
            if (t2449.tag === "SceneMesh") {
                const geometry = t2449.geometry;
                const material = t2449.material;
                if (geometry_handles) {
                    updated_handles.push(Object.assign(new ResourceHandle(), { index: geometry.index, generation: geometry.generation }));
                }
                else {
                    updated_handles.push(Object.assign(new ResourceHandle(), { index: material.index, generation: material.generation }));
                }
            }
            else if (t2449.tag === "SceneGroup") {
            }
            else if (t2449.tag === "SceneCamera") {
            }
            else if (t2449.tag === "SceneLight") {
            }
            else {
                throw new Error("non-exhaustive discerne");
            }
        }
    }
    let traversal = Object.assign(new ResourceHandleTraversal(), { handles: updated_handles, remaining: (remaining - 1) });
    let i = 0;
    while ((i < node.children.length)) {
        const current = _scene_visible_mesh_resource_handles_preorder(store, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(node.children, i), traversal.handles, updated_seen, traversal.remaining, effective_visible, geometry_handles);
        if ((current === null)) {
            return null;
        }
        const current_value = Object.assign(new ResourceHandleTraversal(), { handles: current.handles, remaining: current.remaining });
        traversal = Object.assign(new ResourceHandleTraversal(), { handles: current_value.handles, remaining: current_value.remaining });
        updated_seen.push(Object.assign(new SceneHandle(), { index: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
                throw new Error("index trap"); return __v; })(node.children, i).index, generation: ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
                throw new Error("index trap"); return __v; })(node.children, i).generation }));
        i = (i + 1);
    }
    return traversal;
}
function scene_visible_mesh_geometry_handles(store, root) {
    const traversal = _scene_visible_mesh_resource_handles_preorder(store, root, [], [], store.slots.length, true, true);
    if ((traversal === null)) {
        return null;
    }
    return traversal.handles;
}
function scene_visible_mesh_material_handles(store, root) {
    const traversal = _scene_visible_mesh_resource_handles_preorder(store, root, [], [], store.slots.length, true, false);
    if ((traversal === null)) {
        return null;
    }
    return traversal.handles;
}
function scene_visible_mesh_resource_facts(store, root) {
    const mesh_count_result = scene_visible_mesh_count(store, root);
    if ((mesh_count_result === null)) {
        return null;
    }
    const pair_count_result = scene_visible_mesh_resource_pair_count(store, root);
    if ((pair_count_result === null)) {
        return null;
    }
    const geometry_handles_result = scene_visible_mesh_geometry_handles(store, root);
    if ((geometry_handles_result === null)) {
        return null;
    }
    const material_handles_result = scene_visible_mesh_material_handles(store, root);
    if ((material_handles_result === null)) {
        return null;
    }
    const empty_geometry_handles = [];
    const empty_material_handles = [];
    const geometry_handles = (geometry_handles_result ?? empty_geometry_handles);
    const material_handles = (material_handles_result ?? empty_material_handles);
    const mesh_count = (mesh_count_result ?? 0);
    const pair_count = (pair_count_result ?? 0);
    if ((mesh_count !== pair_count)) {
        return null;
    }
    if ((geometry_handles.length !== mesh_count)) {
        return null;
    }
    if ((material_handles.length !== mesh_count)) {
        return null;
    }
    return Object.assign(new SceneVisibleMeshResourceFacts(), { mesh_count: mesh_count, resource_pair_count: pair_count, geometry_handle_count: geometry_handles.length, material_handle_count: material_handles.length });
}
function scene_visible_mesh_transform_payloads(store, root, view_projection) {
    if ((triga.matrix4_valid(view_projection) === false)) {
        return null;
    }
    const handles = scene_visible_mesh_traverse(store, root);
    if ((handles === null)) {
        return null;
    }
    const empty_handles = [];
    const visible_handles = (handles ?? empty_handles);
    let payloads = [];
    for (const handle of visible_handles) {
        const world = scene_mesh_world_matrix(store, handle);
        if ((world === null)) {
            return null;
        }
        const world_matrix = (world ?? triga.matrix4_identitas());
        const payload = triga.transform_payload(world_matrix, view_projection);
        if ((payload === null)) {
            return null;
        }
        const transform_payload = (payload ?? Object.assign(new any(), { values: [] }));
        payloads.push(Object.assign(new SceneMeshTransformPayload(), { node: Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation }), payload: transform_payload }));
    }
    return payloads;
}
function scene_visible_mesh_transform_payload_count(store, root, view_projection) {
    const payloads = scene_visible_mesh_transform_payloads(store, root, view_projection);
    if ((payloads === null)) {
        return null;
    }
    const empty_payloads = [];
    const visible_payloads = (payloads ?? empty_payloads);
    let count = 0;
    for (const payload of visible_payloads) {
        count = (count + 1);
    }
    return count;
}
function scene_visible_mesh_transform_payload_byte_count(store, root, view_projection) {
    const payloads = scene_visible_mesh_transform_payloads(store, root, view_projection);
    if ((payloads === null)) {
        return null;
    }
    const empty_payloads = [];
    const visible_payloads = (payloads ?? empty_payloads);
    let byte_count = 0;
    for (const payload of visible_payloads) {
        const payload_bytes = triga.transform_payload_byte_count(payload.payload);
        if ((payload_bytes === null)) {
            return null;
        }
        const bytes = (payload_bytes ?? 0);
        byte_count = (byte_count + bytes);
    }
    return byte_count;
}
function scene_visible_mesh_draw_packets(store, root, view_projection) {
    const resources_result = scene_visible_mesh_resources(store, root);
    if ((resources_result === null)) {
        return null;
    }
    const payloads_result = scene_visible_mesh_transform_payloads(store, root, view_projection);
    if ((payloads_result === null)) {
        return null;
    }
    const empty_resources = [];
    const resources = (resources_result ?? empty_resources);
    const empty_payloads = [];
    const payloads = (payloads_result ?? empty_payloads);
    if ((resources.length !== payloads.length)) {
        return null;
    }
    let packets = [];
    let i = 0;
    while ((i < resources.length)) {
        const mesh_resources = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(resources, i);
        const transform_payload = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(payloads, i);
        if ((scene_handle_equals(mesh_resources.node, transform_payload.node) === false)) {
            return null;
        }
        packets.push(Object.assign(new SceneMeshDrawPacket(), { node: Object.assign(new SceneHandle(), { index: mesh_resources.node.index, generation: mesh_resources.node.generation }), geometry: Object.assign(new ResourceHandle(), { index: mesh_resources.geometry.index, generation: mesh_resources.geometry.generation }), material: Object.assign(new ResourceHandle(), { index: mesh_resources.material.index, generation: mesh_resources.material.generation }), transform: transform_payload.payload }));
        i = (i + 1);
    }
    return packets;
}
function scene_visible_mesh_draw_packet_count(store, root, view_projection) {
    const packets_result = scene_visible_mesh_draw_packets(store, root, view_projection);
    if ((packets_result === null)) {
        return null;
    }
    const empty_packets = [];
    const packets = (packets_result ?? empty_packets);
    let count = 0;
    for (const packet of packets) {
        count = (count + 1);
    }
    return count;
}
function scene_visible_mesh_draw_batch_facts(store, root, view_projection) {
    const packet_count_result = scene_visible_mesh_draw_packet_count(store, root, view_projection);
    if ((packet_count_result === null)) {
        return null;
    }
    const transform_bytes_result = scene_visible_mesh_transform_payload_byte_count(store, root, view_projection);
    if ((transform_bytes_result === null)) {
        return null;
    }
    return Object.assign(new SceneVisibleMeshDrawBatchFacts(), { packet_count: (packet_count_result ?? 0), transform_payload_byte_count: (transform_bytes_result ?? 0) });
}
function scene_effective_visible(store, handle) {
    let cursor = Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation });
    let remaining = (store.slots.length + 1);
    while ((remaining > 0)) {
        const current = scene_get(store, cursor);
        if ((current === null)) {
            return false;
        }
        if ((current.visible === false)) {
            return false;
        }
        if ((current.parent === null)) {
            return true;
        }
        const parent = Object.assign(new SceneHandle(), { index: current.parent.index, generation: current.parent.generation });
        cursor = Object.assign(new SceneHandle(), { index: parent.index, generation: parent.generation });
        remaining = (remaining - 1);
    }
    return false;
}
function scene_find_name(store, root, name) {
    const result = _scene_traverse_preorder(store, root, [], store.slots.length);
    if ((result === null)) {
        return null;
    }
    const traversal = Object.assign(new SceneTraversal(), { handles: result.handles, remaining: result.remaining });
    let i = 0;
    while ((i < traversal.handles.length)) {
        const handle = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(traversal.handles, i);
        const node = scene_get(store, handle);
        if ((node === null)) {
            return null;
        }
        if ((node.name === name)) {
            return Object.assign(new SceneHandle(), { index: handle.index, generation: handle.generation });
        }
        i = (i + 1);
    }
    return null;
}
function _scene_replace(store, handle, node) {
    let slots = [];
    let i = 0;
    while ((i < store.slots.length)) {
        const slot = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(store.slots, i);
        if ((((i === handle.index) && slot.alive) && (slot.generation === handle.generation))) {
            slots.push(Object.assign(new SceneSlot(), { generation: slot.generation, alive: true, node: node }));
        }
        else {
            slots.push(slot);
        }
        i = (i + 1);
    }
    return Object.assign(new SceneStore(), { slots: slots });
}
function _scene_without_child(parent, child) {
    let children = [];
    let i = 0;
    while ((i < parent.children.length)) {
        const existing = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(parent.children, i);
        if ((scene_handle_equals(existing, child) === false)) {
            children.push(Object.assign(new SceneHandle(), { index: existing.index, generation: existing.generation }));
        }
        i = (i + 1);
    }
    return Object.assign(new SceneNode(), { name: parent.name, kind: parent.kind, parent: parent.parent, children: children, visible: parent.visible, local_matrix: parent.local_matrix, world_matrix: parent.world_matrix, world_dirty: parent.world_dirty });
}
function _scene_would_cycle(store, child, parent) {
    let cursor = Object.assign(new SceneHandle(), { index: parent.index, generation: parent.generation });
    let remaining = (store.slots.length + 1);
    while ((remaining > 0)) {
        if (scene_handle_equals(cursor, child)) {
            return true;
        }
        const current = scene_get(store, cursor);
        if ((current === null)) {
            return false;
        }
        const current_node = Object.assign(new SceneNode(), { name: current.name, kind: current.kind, parent: current.parent, children: current.children, visible: current.visible, local_matrix: current.local_matrix, world_matrix: current.world_matrix, world_dirty: current.world_dirty });
        if ((current_node.parent === null)) {
            return false;
        }
        const current_parent = Object.assign(new SceneHandle(), { index: current_node.parent.index, generation: current_node.parent.generation });
        cursor = Object.assign(new SceneHandle(), { index: current_parent.index, generation: current_parent.generation });
        remaining = (remaining - 1);
    }
    return true;
}
function scene_attach(store, child, parent) {
    if ((scene_contains(store, child) === false)) {
        return null;
    }
    if ((scene_contains(store, parent) === false)) {
        return null;
    }
    if (_scene_would_cycle(store, child, parent)) {
        return null;
    }
    const child_result = scene_get(store, child);
    const parent_result = scene_get(store, parent);
    const child_node = Object.assign(new SceneNode(), { name: child_result.name, kind: child_result.kind, parent: child_result.parent, children: child_result.children, visible: child_result.visible, local_matrix: child_result.local_matrix, world_matrix: child_result.world_matrix, world_dirty: child_result.world_dirty });
    const parent_node = Object.assign(new SceneNode(), { name: parent_result.name, kind: parent_result.kind, parent: parent_result.parent, children: parent_result.children, visible: parent_result.visible, local_matrix: parent_result.local_matrix, world_matrix: parent_result.world_matrix, world_dirty: parent_result.world_dirty });
    if ((child_node.parent !== null)) {
        const existing_parent = Object.assign(new SceneHandle(), { index: child_node.parent.index, generation: child_node.parent.generation });
        if (scene_handle_equals(existing_parent, parent)) {
            return null;
        }
    }
    let children = [];
    let i = 0;
    while ((i < parent_node.children.length)) {
        const existing = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(parent_node.children, i);
        children.push(Object.assign(new SceneHandle(), { index: existing.index, generation: existing.generation }));
        i = (i + 1);
    }
    children.push(Object.assign(new SceneHandle(), { index: child.index, generation: child.generation }));
    const updated_parent = Object.assign(new SceneNode(), { name: parent_node.name, kind: parent_node.kind, parent: parent_node.parent, children: children, visible: parent_node.visible, local_matrix: parent_node.local_matrix, world_matrix: parent_node.world_matrix, world_dirty: parent_node.world_dirty });
    const updated_child = Object.assign(new SceneNode(), { name: child_node.name, kind: child_node.kind, parent: Object.assign(new SceneHandle(), { index: parent.index, generation: parent.generation }), children: child_node.children, visible: child_node.visible, local_matrix: child_node.local_matrix, world_matrix: child_node.world_matrix, world_dirty: true });
    let without_old_parent = Object.assign(new SceneStore(), { slots: store.slots });
    if ((child_node.parent !== null)) {
        const old_parent_handle = Object.assign(new SceneHandle(), { index: child_node.parent.index, generation: child_node.parent.generation });
        const old_parent_result = scene_get(store, old_parent_handle);
        if ((old_parent_result === null)) {
            return null;
        }
        const old_parent = Object.assign(new SceneNode(), { name: old_parent_result.name, kind: old_parent_result.kind, parent: old_parent_result.parent, children: old_parent_result.children, visible: old_parent_result.visible, local_matrix: old_parent_result.local_matrix, world_matrix: old_parent_result.world_matrix, world_dirty: old_parent_result.world_dirty });
        without_old_parent = _scene_replace(store, old_parent_handle, _scene_without_child(old_parent, child));
    }
    const with_parent = _scene_replace(without_old_parent, parent, updated_parent);
    const with_child = _scene_replace(with_parent, child, updated_child);
    return _scene_mark_world_dirty(with_child, child);
}
function scene_detach(store, child) {
    if ((scene_contains(store, child) === false)) {
        return null;
    }
    const child_result = scene_get(store, child);
    const child_node = Object.assign(new SceneNode(), { name: child_result.name, kind: child_result.kind, parent: child_result.parent, children: child_result.children, visible: child_result.visible, local_matrix: child_result.local_matrix, world_matrix: child_result.world_matrix, world_dirty: child_result.world_dirty });
    if ((child_node.parent === null)) {
        return null;
    }
    const parent_handle = Object.assign(new SceneHandle(), { index: child_node.parent.index, generation: child_node.parent.generation });
    const parent_result = scene_get(store, parent_handle);
    if ((parent_result === null)) {
        return null;
    }
    const parent_node = Object.assign(new SceneNode(), { name: parent_result.name, kind: parent_result.kind, parent: parent_result.parent, children: parent_result.children, visible: parent_result.visible, local_matrix: parent_result.local_matrix, world_matrix: parent_result.world_matrix, world_dirty: parent_result.world_dirty });
    const detached_child = Object.assign(new SceneNode(), { name: child_node.name, kind: child_node.kind, children: child_node.children, visible: child_node.visible, local_matrix: child_node.local_matrix, world_matrix: child_node.world_matrix, world_dirty: true });
    const without_child = _scene_replace(store, parent_handle, _scene_without_child(parent_node, child));
    const with_child = _scene_replace(without_child, child, detached_child);
    return _scene_mark_world_dirty(with_child, child);
}
function _scene_mark_world_dirty(store, handle) {
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    let updated = _scene_replace(store, handle, Object.assign(new SceneNode(), { name: node.name, kind: node.kind, parent: node.parent, children: node.children, visible: node.visible, local_matrix: node.local_matrix, world_matrix: node.world_matrix, world_dirty: true }));
    let i = 0;
    while ((i < node.children.length)) {
        const current = _scene_mark_world_dirty(updated, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(node.children, i));
        if ((current !== null)) {
            updated = Object.assign(new SceneStore(), { slots: current.slots });
        }
        else {
            return null;
        }
        i = (i + 1);
    }
    return updated;
}
function scene_set_visible(store, handle, visible) {
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    return _scene_replace(store, handle, Object.assign(new SceneNode(), { name: node.name, kind: node.kind, parent: node.parent, children: node.children, visible: visible, local_matrix: node.local_matrix, world_matrix: node.world_matrix, world_dirty: node.world_dirty }));
}
function scene_set_local_matrix(store, handle, local_matrix) {
    if ((local_matrix.elements.length !== 16)) {
        return null;
    }
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    const with_local = _scene_replace(store, handle, Object.assign(new SceneNode(), { name: node.name, kind: node.kind, parent: node.parent, children: node.children, visible: node.visible, local_matrix: local_matrix, world_matrix: node.world_matrix, world_dirty: true }));
    return _scene_mark_world_dirty(with_local, handle);
}
function _scene_update_world_node(store, handle, parent_world) {
    const node_result = scene_get(store, handle);
    if ((node_result === null)) {
        return null;
    }
    const node = Object.assign(new SceneNode(), { name: node_result.name, kind: node_result.kind, parent: node_result.parent, children: node_result.children, visible: node_result.visible, local_matrix: node_result.local_matrix, world_matrix: node_result.world_matrix, world_dirty: node_result.world_dirty });
    const world = triga.matrix4_multiplicata(parent_world, node.local_matrix);
    let updated = _scene_replace(store, handle, Object.assign(new SceneNode(), { name: node.name, kind: node.kind, parent: node.parent, children: node.children, visible: node.visible, local_matrix: node.local_matrix, world_matrix: world, world_dirty: false }));
    let i = 0;
    while ((i < node.children.length)) {
        const current = _scene_update_world_node(updated, ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(node.children, i), world);
        if ((current !== null)) {
            updated = Object.assign(new SceneStore(), { slots: current.slots });
        }
        else {
            return null;
        }
        i = (i + 1);
    }
    return updated;
}
function scene_update_world(store, root) {
    const root_result = scene_get(store, root);
    if ((root_result === null)) {
        return null;
    }
    if ((root_result.parent !== null)) {
        return null;
    }
    return _scene_update_world_node(store, root, triga.matrix4_identitas());
}
function scene_remove(store, handle) {
    if ((scene_contains(store, handle) === false)) {
        return null;
    }
    const node_result = scene_get(store, handle);
    if ((node_result.children.length > 0)) {
        return null;
    }
    let detached_store = Object.assign(new SceneStore(), { slots: store.slots });
    if ((node_result.parent !== null)) {
        const current = scene_detach(store, handle);
        if ((current !== null)) {
            detached_store = Object.assign(new SceneStore(), { slots: current.slots });
        }
        else {
            return null;
        }
    }
    let slots = [];
    let i = 0;
    while ((i < detached_store.slots.length)) {
        const slot = ((__o, __i) => { const __v = __o[__i]; if (__v === undefined)
            throw new Error("index trap"); return __v; })(detached_store.slots, i);
        if ((i === handle.index)) {
            slots.push(Object.assign(new SceneSlot(), { generation: (slot.generation + 1), alive: false, node: slot.node }));
        }
        else {
            slots.push(slot);
        }
        i = (i + 1);
    }
    return Object.assign(new SceneStore(), { slots: slots });
}
export const scene = {
    ResourceHandle,
    ResourceHandleTraversal,
    ResourceLifecycleBatchFacts,
    ResourceLifecycleTransition,
    ResourceTransition,
    SceneCamera,
    SceneCount,
    SceneGroup,
    SceneHandle,
    SceneInsert,
    SceneLight,
    SceneMesh,
    SceneMeshDrawPacket,
    SceneMeshResourceTraversal,
    SceneMeshResources,
    SceneMeshTransformPayload,
    SceneNode,
    SceneSlot,
    SceneStore,
    SceneTraversal,
    SceneVisibleMeshDrawBatchFacts,
    SceneVisibleMeshResourceFacts,
    _resource_lifecycle_index_seen,
    _resource_transition_index_seen,
    _scene_count_visible_mesh_preorder,
    _scene_mark_world_dirty,
    _scene_replace,
    _scene_traversal_contains,
    _scene_traverse_preorder,
    _scene_traverse_visible_mesh_preorder,
    _scene_traverse_visible_mesh_resources_preorder,
    _scene_traverse_visible_preorder,
    _scene_update_world_node,
    _scene_visible_mesh_resource_handles_preorder,
    _scene_without_child,
    _scene_would_cycle,
    resource_handle_equals,
    resource_handle_next,
    resource_lifecycle_batch_facts,
    resource_lifecycle_changed_count,
    resource_lifecycle_changed_handles,
    resource_lifecycle_changed_indices,
    resource_lifecycle_created,
    resource_lifecycle_created_count,
    resource_lifecycle_current_handles,
    resource_lifecycle_live_count,
    resource_lifecycle_live_indices,
    resource_lifecycle_removed,
    resource_lifecycle_removed_count,
    resource_lifecycle_removed_handles,
    resource_lifecycle_removed_indices,
    resource_lifecycle_replaced,
    resource_lifecycle_retired_handles,
    resource_lifecycle_retired_indices,
    resource_lifecycle_unchanged,
    resource_lifecycle_unchanged_count,
    resource_lifecycle_unchanged_handles,
    resource_lifecycle_unchanged_indices,
    resource_lifecycle_valid,
    resource_lifecycles_valid,
    resource_transition_changed_count,
    resource_transition_changed_handles,
    resource_transition_changed_indices,
    resource_transition_current_handles,
    resource_transition_replaced,
    resource_transition_unchanged,
    resource_transition_unchanged_count,
    resource_transition_unchanged_handles,
    resource_transition_unchanged_indices,
    resource_transition_valid,
    resource_transitions_valid,
    scene_attach,
    scene_camera,
    scene_contains,
    scene_detach,
    scene_effective_visible,
    scene_find_name,
    scene_get,
    scene_group,
    scene_handle_equals,
    scene_insert,
    scene_light,
    scene_mesh,
    scene_mesh_geometry,
    scene_mesh_material,
    scene_mesh_world_matrix,
    scene_node,
    scene_remove,
    scene_set_local_matrix,
    scene_set_visible,
    scene_store,
    scene_traverse,
    scene_update_world,
    scene_visible_mesh_count,
    scene_visible_mesh_draw_batch_facts,
    scene_visible_mesh_draw_packet_count,
    scene_visible_mesh_draw_packets,
    scene_visible_mesh_geometry_handles,
    scene_visible_mesh_material_handles,
    scene_visible_mesh_resource_facts,
    scene_visible_mesh_resource_pair_count,
    scene_visible_mesh_resources,
    scene_visible_mesh_transform_payload_byte_count,
    scene_visible_mesh_transform_payload_count,
    scene_visible_mesh_transform_payloads,
    scene_visible_mesh_traverse,
    scene_visible_traverse,
};
