// Generated by radix - do not edit
export function __faberDisplay(value, hint = "unknown") {
    if (typeof hint === "object") {
        if (hint.kind === "nullable") {
            return value === null || value === undefined ? "nihil" : __faberDisplay(value, hint.inner);
        }
        if (hint.kind === "lista") {
            return __faberDisplayList(value, hint.element);
        }
        if (hint.kind === "tensor") {
            return __faberDisplayTensor(value, hint.element);
        }
        if (hint.kind === "sparsa") {
            return __faberDisplaySparsa(value, hint.element);
        }
        if (hint.kind === "map") {
            return __faberDisplayMap(value, hint.key, hint.value);
        }
        return __faberDisplayList(value, hint.element);
    }
    if (value === null || value === undefined) {
        return hint === "vacuum" ? "vacuum" : "nihil";
    }
    switch (hint) {
        case "bivalens": return value ? "verum" : "falsum";
        case "fractus": return __faberDisplayFractus(value);
        case "nihil": return "nihil";
        case "vacuum": return "vacuum";
        case "valor": return __faberDisplayValor(value);
        case "regex": return String(value);
        case "textus":
        case "ascii":
        case "instans":
        case "numerus": return String(value);
        default: return __faberDisplayValor(value);
    }
}
export function __faberDisplayFractus(value) {
    const n = Number(value);
    if (!Number.isFinite(n)) {
        return String(value);
    }
    return Number.isInteger(n) ? `${n}.0` : String(n);
}
export function __faberDisplayList(value, element) {
    if (value === null || value === undefined) {
        return "nihil";
    }
    return `[${Array.from(value).map((item) => __faberDisplay(item, element)).join(", ")}]`;
}
export function __faberDisplayTensor(value, element) {
    if (value === null || value === undefined) {
        return "nihil";
    }
    if (typeof value.planata === "function") {
        return __faberDisplayList(value.planata(), element);
    }
    if (Array.isArray(value.data)) {
        return __faberDisplayList(value.data, element);
    }
    return __faberDisplayValor(value);
}
export function __faberDisplaySparsa(value, element) {
    if (value === null || value === undefined) {
        return "nihil";
    }
    if (typeof value.densata === "function") {
        return __faberDisplayTensor(value.densata(), element);
    }
    return __faberDisplayValor(value);
}
export function __faberDisplayMap(value, keyHint, valueHint) {
    if (value === null || value === undefined) {
        return "nihil";
    }
    const entries = value instanceof Map ? Array.from(value.entries()) : Object.entries(value);
    return `{${entries.map(([key, item]) => `${JSON.stringify(__faberDisplay(key, keyHint))}: ${__faberDisplay(item, valueHint)}`).join(", ")}}`;
}
export function __faberDisplayValor(value) {
    if (value !== null && typeof value === "object" && typeof value.__faberValorTag === "string") {
        return __faberDisplayTaggedValor(value);
    }
    if (value === null || value === undefined) {
        return "nihil";
    }
    if (typeof value === "boolean") {
        return value ? "verum" : "falsum";
    }
    if (typeof value === "number") {
        return String(value);
    }
    if (typeof value === "string") {
        return value;
    }
    if (Array.isArray(value)) {
        return __faberDisplayList(value, "valor");
    }
    if (typeof value.text === "function") {
        return String(value.text());
    }
    if (typeof value.toString === "function" && value.toString !== Object.prototype.toString) {
        return String(value);
    }
    return __faberDisplayMap(value, "textus", "valor");
}
export function __faberDisplayTaggedValor(value) {
    const payload = value.__faberValorPayload;
    switch (value.__faberValorTag) {
        case "Nihil": return "Nihil";
        case "Bivalens": return `Bivalens(${payload})`;
        case "Numerus": return `Numerus(${payload})`;
        case "Fractus": return `Fractus(${__faberDisplayFractus(payload)})`;
        case "Textus": return `Textus(${JSON.stringify(String(payload))})`;
        case "Instans": return `Instans(${JSON.stringify(String(payload))})`;
        case "Octeti": return `Octeti(${JSON.stringify(payload)})`;
        case "Lista": return `Lista(${__faberDisplayValorPayloadList(payload)})`;
        case "Tabula": return `Tabula(${__faberDisplayValorPayloadMap(payload)})`;
        default: return __faberDisplayValor(payload);
    }
}
export function __faberDisplayValorPayload(value) {
    if (value === null || value === undefined) {
        return "Nihil";
    }
    if (typeof value === "boolean") {
        return `Bivalens(${value})`;
    }
    if (typeof value === "number") {
        return `${Number.isInteger(value) ? "Numerus" : "Fractus"}(${Number.isInteger(value) ? value : __faberDisplayFractus(value)})`;
    }
    if (typeof value === "string") {
        return `Textus(${JSON.stringify(value)})`;
    }
    if (Array.isArray(value)) {
        return `Lista(${__faberDisplayValorPayloadList(value)})`;
    }
    if (typeof value.planata === "function") {
        return `Lista(${__faberDisplayValorPayloadList(value.planata())})`;
    }
    if (typeof value.text === "function") {
        return `Instans(${JSON.stringify(String(value.text()))})`;
    }
    return `Tabula(${__faberDisplayValorPayloadMap(value)})`;
}
export function __faberDisplayValorPayloadList(value) {
    return `[${Array.from(value).map((item) => __faberDisplayValorPayload(item)).join(", ")}]`;
}
export function __faberDisplayValorPayloadMap(value) {
    const entries = value instanceof Map ? Array.from(value.entries()) : Object.entries(value);
    return `{${entries.map(([key, item]) => `${JSON.stringify(String(key))}: ${__faberDisplayValorPayload(item)}`).join(", ")}}`;
}
export class Scope {
    selector = "";
}
export class Element {
    selector = "";
}
export class DomEvent {
    kind = "";
    default_prevented = false;
}
export class FrameState {
    frame = 0;
    time_ms = 0;
    delta_ms = 0;
}
export class ResizeState {
    width = 0;
    height = 0;
    device_pixel_ratio = 1;
}
export class KeyboardState {
    kind = "";
    key = "";
    code = "";
    repeat = false;
    alt = false;
    ctrl = false;
    shift = false;
    meta = false;
}
export class PointerState {
    kind = "";
    x = 0;
    y = 0;
    movement_x = 0;
    movement_y = 0;
    button = 0;
    primary = false;
}
export class FocusState {
    focused = false;
}
export class PointerLockState {
    supported = false;
    locked = false;
    denied = false;
    target_matches = false;
}
export class Subscription {
    id = 0;
}
export class SubmitOptions {
    prevent_default = true;
}
export class FetchRequest {
    url;
    method = "GET";
    body;
}
export class FetchResponse {
    status = 0;
    ok = false;
    body = "";
}
export function scope(selector) {
    return Object.assign(new Scope(), { selector: selector });
}
export function element(selector) {
    return Object.assign(new Element(), { selector: selector });
}
export function query(scope, selector) {
    console.log(__faberDisplay(scope, "unknown"));
    console.log(__faberDisplay(selector, "textus"));
    return null;
}
export function require(scope, selector) {
    console.log(__faberDisplay(scope, "unknown"));
    return Object.assign(new Element(), { selector: selector });
}
export function all(scope, selector) {
    console.log(__faberDisplay(scope, "unknown"));
    console.log(__faberDisplay(selector, "textus"));
    const elements = [];
    return elements;
}
export function text_set(element, value) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(value, "textus"));
}
export function attr_set(element, name, value) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(name, "textus"));
    console.log(__faberDisplay(value, "textus"));
}
export function attr_remove(element, name) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(name, "textus"));
}
export function class_add(element, class_name) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(class_name, "textus"));
}
export function class_remove(element, class_name) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(class_name, "textus"));
}
export function class_toggle(element, class_name) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(class_name, "textus"));
}
export function on(element, event_name, handler) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(event_name, "textus"));
    console.log(__faberDisplay(handler, "unknown"));
    return Object.assign(new Subscription(), { id: 0 });
}
export function unsubscribe(subscription) {
    console.log(__faberDisplay(subscription, "unknown"));
}
export function value(element) {
    console.log(__faberDisplay(element, "unknown"));
    return "";
}
export function value_set(element, value) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(value, "textus"));
}
export function on_input(element, handler) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(handler, "unknown"));
    return Object.assign(new Subscription(), { id: 0 });
}
export function on_submit(element, options, handler) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(options, "unknown"));
    console.log(__faberDisplay(handler, "unknown"));
    return Object.assign(new Subscription(), { id: 0 });
}
export function on_frame(handler) {
    console.log(__faberDisplay(handler, "unknown"));
    return Object.assign(new Subscription(), { id: 0 });
}
export function on_resize(handler) {
    console.log(__faberDisplay(handler, "unknown"));
    return Object.assign(new Subscription(), { id: 0 });
}
export function on_keyboard(element, event_name, handler) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(event_name, "textus"));
    console.log(__faberDisplay(handler, "unknown"));
    return Object.assign(new Subscription(), { id: 0 });
}
export function on_pointer(element, event_name, handler) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(event_name, "textus"));
    console.log(__faberDisplay(handler, "unknown"));
    return Object.assign(new Subscription(), { id: 0 });
}
export function on_focus(element, event_name, handler) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(event_name, "textus"));
    console.log(__faberDisplay(handler, "unknown"));
    return Object.assign(new Subscription(), { id: 0 });
}
export function pointer_lock_state(element) {
    console.log(__faberDisplay(element, "unknown"));
    return Object.assign(new PointerLockState(), { supported: false, locked: false, denied: false, target_matches: false });
}
export function request_pointer_lock(element) {
    console.log(__faberDisplay(element, "unknown"));
    return Object.assign(new PointerLockState(), { supported: false, locked: false, denied: true, target_matches: false });
}
export function exit_pointer_lock() {
    return Object.assign(new PointerLockState(), { supported: false, locked: false, denied: false, target_matches: false });
}
export function on_pointer_lock(element, handler) {
    console.log(__faberDisplay(element, "unknown"));
    console.log(__faberDisplay(handler, "unknown"));
    return Object.assign(new Subscription(), { id: 0 });
}
export function prevent_default(event) {
    return Object.assign(new DomEvent(), { kind: event.kind, default_prevented: true });
}
async function fetch_text(request) {
    console.log(__faberDisplay(request, "unknown"));
    return { ok: true, value: Object.assign(new FetchResponse(), { status: 0, ok: false, body: "" }) };
}
export const dom = {
    __faberDisplay,
    __faberDisplayFractus,
    __faberDisplayList,
    __faberDisplayTensor,
    __faberDisplaySparsa,
    __faberDisplayMap,
    __faberDisplayValor,
    __faberDisplayTaggedValor,
    __faberDisplayValorPayload,
    __faberDisplayValorPayloadList,
    __faberDisplayValorPayloadMap,
    Scope,
    Element,
    DomEvent,
    FrameState,
    ResizeState,
    KeyboardState,
    PointerState,
    FocusState,
    PointerLockState,
    Subscription,
    SubmitOptions,
    FetchRequest,
    FetchResponse,
    scope,
    element,
    query,
    require,
    all,
    text_set,
    attr_set,
    attr_remove,
    class_add,
    class_remove,
    class_toggle,
    on,
    unsubscribe,
    value,
    value_set,
    on_input,
    on_submit,
    on_frame,
    on_resize,
    on_keyboard,
    on_pointer,
    on_focus,
    pointer_lock_state,
    request_pointer_lock,
    exit_pointer_lock,
    on_pointer_lock,
    prevent_default
};
