# Script-kernel exempla

Programs that exercise the `faber:*` script-kernel import surface.

Public product track for the Faber CLI script host; not keyword dictionary
entries (`examples/corpus/`) and not Norma (`norma:*`).
